// identity/services/ — identity-layer business logic.
//
// Post-Phase-2A.2 contents:
//   - contact.service.ts (from services/)
//   - participant.service.ts (from services/)
//   - auth.service.ts (from services/)
//   - agent-visibility.ts (from services/)
//   - session-validator.ts (from services/)
//   - agent-auth.ts (from auth/agent-auth.ts; key/secret utilities)
//
// Note on services not named in parent epic mapping:
// `auth.service.ts`, `agent-visibility.ts`, `session-validator.ts`
// land here per §2 Modules disposition (architect call). The parent
// epic mapping table cited `contact-policy.service.ts` and
// `conversation-admin-authority.ts` which do not exist in main —
// recorded as Risk #5 (services/ split judgment) in the Phase 2A plan.

export {};
