// identity/ — registration, auth, contacts.
//
// Public barrel for the identity layer. Post-Phase-2A.2, this layer
// owns auth handlers (registration, claim, login) and identity-side
// services (contact, participant, agent-visibility, auth.service,
// session-validator). Skeleton stage: empty; symbols land in 2A.2.
//
// Folder shape (per Q-task-folder-shape resolution): handlers/ and
// services/ are peers under identity/, alongside identity/index.ts.

export {};
