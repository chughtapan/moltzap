/** Provides the integration hook timeout ms runtime value. */
export const INTEGRATION_HOOK_TIMEOUT_MS = 60_000;
/** Provides the message settle ms runtime value. */
export const MESSAGE_SETTLE_MS = 500;
/** Provides the notification wait ms runtime value. */
export const NOTIFICATION_WAIT_MS = 5_000;
/** Provides the history settle ms runtime value. */
export const HISTORY_SETTLE_MS = 2_000;
/** Provides the long message length runtime value. */
export const LONG_MESSAGE_LENGTH = 500;
/** Provides the history message count runtime value. */
export const HISTORY_MESSAGE_COUNT = 25;
/** Provides the socket response timeout ms runtime value. */
export const SOCKET_RESPONSE_TIMEOUT_MS = 2_000;
/** Provides the context limit runtime value. */
export const CONTEXT_LIMIT = 2;
/** Provides the socket history limit runtime value. */
export const SOCKET_HISTORY_LIMIT = 10;
/** Provides the socket page message count runtime value. */
export const SOCKET_PAGE_MESSAGE_COUNT = 3;
/** Provides the service name test runtime value. */
export const SERVICE_NAME_TEST = "name-test";
/** Provides the hello from c runtime value. */
export const HELLO_FROM_C = "Hello from C";
/** Provides the hello receiver runtime value. */
export const HELLO_RECEIVER = "Hello receiver";
/** Provides the hello from service runtime value. */
export const HELLO_FROM_SERVICE = "Hello from service";
/** Provides the shared update runtime value. */
export const SHARED_UPDATE = "Shared update";
/** Provides the b update runtime value. */
export const B_UPDATE = "B update";
/** Provides the from c runtime value. */
export const FROM_C = "From C";
/** Provides the from d runtime value. */
export const FROM_D = "From D";
/** Provides the first message runtime value. */
export const FIRST_MESSAGE = "First";
/** Provides the second message runtime value. */
export const SECOND_MESSAGE = "Second";
/** Provides the new message runtime value. */
export const NEW_MESSAGE = "new msg";
/** Provides the price message runtime value. */
export const PRICE_MESSAGE = "Price is $4000";
/** Provides the track session key runtime value. */
export const TRACK_SESSION_KEY = "track-test-session";
/** Provides the track new message runtime value. */
export const TRACK_NEW_MESSAGE = "track-msg-new";
/** Provides the image marker runtime value. */
export const IMAGE_MARKER = "[image]";
/** Provides the system reminder open runtime value. */
export const SYSTEM_REMINDER_OPEN = "<system-reminder>";
/** Provides the system reminder close runtime value. */
export const SYSTEM_REMINDER_CLOSE = "</system-reminder>";
/** Provides the one new marker runtime value. */
export const ONE_NEW_MARKER = "(1 new)";
/** Provides the resolved agent context name runtime value. */
export const RESOLVED_AGENT_CONTEXT_NAME = "@name-res-c";
/** Provides the history first buffer message runtime value. */
export const HISTORY_FIRST_BUFFER_MESSAGE = "msg-0";
/** Provides the history last buffer message runtime value. */
export const HISTORY_LAST_BUFFER_MESSAGE = "msg-24";
/** Provides the peek from c runtime value. */
export const PEEK_FROM_C = "from C";
/** Provides the history participant count runtime value. */
export const HISTORY_PARTICIPANT_COUNT = 3;
/** Provides the sock hist b name runtime value. */
export const SOCK_HIST_B_NAME = "sock-hist-b";
