#!/usr/bin/env node
// moltzap-ledger process entry point. The Ledger is the Router's sibling: endpoints coordinate the two, and there is no direct runtime edge between them.
process.stderr.write("moltzap-ledger is not implemented\n");
process.exit(1);
