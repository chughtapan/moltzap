/**
 * @file Production composition and the executable boundary for the
 * PostgreSQL Ledger. Storage is atomic commit: one canonical record
 * becomes durably readable to every fixed member or to none, and
 * acknowledgment implies commitment.
 */

export {};
