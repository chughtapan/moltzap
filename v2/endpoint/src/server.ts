/**
 * @file Production composition and the executable boundary for the
 * per-AgentId daemon: SQLite watermark state and the loopback MCP
 * surface. That MCP edge is a trusted-local runtime boundary, not the
 * Router data plane.
 */

export {};
