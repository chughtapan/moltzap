/**
 * @file Public endpoint capabilities. Everything interpretive lives here:
 * the protocol engine, `OpenFloorV1`, recovery and reconciliation, and the
 * decision about which certificates to sign. Endpoints are the only place
 * that interprets message bodies.
 */

export {};
