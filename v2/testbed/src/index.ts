/**
 * @file The production `StackProvider` Live Layer: platform acquisition,
 * production-process supervision, fault layers, substitutes,
 * external-runtime constructors, and black-box subjects.
 *
 * Testbed supplies a concrete platform to the simulator's contract. It
 * never owns a production service and never wraps an umbrella server.
 * Nothing in production depends on this package.
 */

export {};
