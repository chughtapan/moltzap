# moltzap v1 Debt Inventory — 2026-07-18

Five read-only auditors: issues, architecture enforcement, docs infra, conformance, CI/release guardrails.

## open GitHub issues (chughtapan/moltzap)

**Current state:** 43 open issues. Disposition counts: superseded-by-v2 = 35, fix-in-v1 = 5 (#277, #670, #715, #190, #191), tighten-now = 2 (#733, #482), close-obsolete = 1 (#402, verified: all 12 sub-issues #403–#412 are CLOSED, the epic was simply never closed). The backlog is dominated by v1 architecture/refactor plans (#750, #745, #724, #718, #737), missing protocol features tracked as conformance-tombstone epics (#245, #246, #247), lifecycle-conformance spec stubs (#334/#335/#336/#338), the dormant propagation-experiment cluster (#360–#365), and claude-code-channel capability gaps (#442–#445). Genuinely live v1 items are few: the published-package zero-env boot regression (#277, `packages/server/src/config.ts` / `standalone.ts` devMode path), the TaskCreate dedup hot-path perf issue with a written one-query fix sketch (#670, `task.service.ts → findExistingTaskByParticipants`), the registration-route dual-exposure config split (#715), and the arena dogfood operator cleanup (#190/#191, last touched 2026-07-11).

**Gaps:**
- Completed work left open: #402's entire 12-step decomposition is closed but the epic is open — tracker state has drifted from reality.
- No label separates 'v1 defect' from 'protocol/feature requirement' — issues like #247/#246/#245 (features that do not exist) and #280/#279 (labeled 'bug' but actually SDK surface requests) read as bugs but are v2 spec inputs; a v2-input label is needed before the rewrite branch scaffolds.
- #745 (post-#722 modernization roadmap) is a mid-flight 9-step sequenced plan executing on a pre-merge branch; nobody has decided which steps still execute on v1 vs. move wholesale into v2 — highest-risk drift point in the tracker.
- Dormant clusters with no state signal: propagation experiment #360–#365 (untouched since 2026-05-02) and lifecycle conformance #334–#338 (since 2026-05-01) sit in 'planning' with no owner or defer marker.
- #277's four symptoms were reported against server-core 2026.425.4; recent quickstart commits (aec00a46) may have fixed a subset — the issue needs re-verification against the current release before any fix work.
- #733's lint rule (eslint-rules/no-long-inline-comment.mjs) is authored but UNCOMMITTED per the issue body — enforcement value is zero until the capstone commit lands.

**Actions:**
- **[superseded-by-v2 / S]** #750 App/ surface simplification (architect plan) — clean-slate v2 rebuilds the app-principal surface; carry the plan's hard constraints (manifest JSON-schema byte-stability, no runtime-map+cast tuples) into the v2 app spec instead of executing the refactor on v1.
- **[superseded-by-v2 / S]** #747 Schema-driven CLI from Effect schemas — v2 day-one design choice: descriptor schemas as single source of truth generate CLI options; do not retrofit v1's hand-defined options.
- **[superseded-by-v2 / S]** #745 Post-#722 modernization roadmap (durable) — the v2 rewrite IS this roadmap's replacement; convert its 9-step sequence (transport reorg, descriptor modernization, envelope drop, transport extraction) into the v2 scaffold checklist and explicitly cancel remaining v1 execution.
- **[superseded-by-v2 / S]** #738 Compile-time guard: handler error ⊆ wire-error union — the issue itself proves the guard is unsound to retrofit (EngineRpcFromDef type-erases the error slot); make it sound-by-construction in v2's RPC descriptor typing, with the three documented runtime bites as the motivating test cases.
- **[superseded-by-v2 / S]** #737 Simplification audit — 280 candidate facts — executing 280 simplifications on code v2 deletes is wasted churn; feed .claude/simplification-audit/facts.json (esp. the 13 arch-linter violations and 85 do-first facts) into the v2 module-layout spec as 'what not to rebuild'.
- **[tighten-now / M]** #733 Comment cold-reader discipline + no-long-inline-comment lint rule — the rule is authored but uncommitted; land the capstone commit and wire it as an error-level gate now so comment rot cannot re-accumulate in v1 tail work OR on the v2 branch.
- **[superseded-by-v2 / S]** #724 Separate server/app: boot vs app-principal — a folder-layout contract for v1 packages/server/src/app/; v2 draws this boundary from scratch, so the contract's goals (cold-reader separation of composition root vs principal layer) become v2 module-layout inputs.
- **[superseded-by-v2 / S]** #718 First-class conversation-subscription index — retiring the per-connection conversationIds Set is a server-state redesign; v2's connection/subscription model should own membership as a single-source index from day one rather than redesigning v1 twice.
- **[fix-in-v1 / S]** #715 Split skipDefaultRegisterRoute into per-route flags — small contained config change closing a real operator dual-exposure window (agent vs app registration) on deployed v1; worth fixing regardless of v2.
- **[superseded-by-v2 / S]** #684 Reap stale waiting tasks from task/create callback window — explicitly not a correctness hazard (stranded rows are invisible to delivery); v2 should make task creation atomic across the TM callback or own the reaper by design.
- **[fix-in-v1 / M]** #670 TaskCreate dedup O(N) round-trips + double contact-policy — live hot-path perf on every task/create while v1 serves traffic, with a written single-SQL-query fix sketch (join tasks + task_participants with HAVING); contained enough to fix in v1.
- **[superseded-by-v2 / S]** #638 JSDoc HTML-entity sweep (56 files) — cosmetic debt in files v2 replaces; carry the rule (no &lt;/&gt;/&amp; in JSDoc, per the project's Mermaid/JSDoc gotchas doc) into v2 authoring standards or a lint instead of sweeping v1.
- **[superseded-by-v2 / S]** #569 Task GC policy for active tasks — pure unanswered lifecycle spec question (when does active transition without tasks/close); answer it in the v2 task-lifecycle spec.
- **[tighten-now / S]** #482 PR body conventions (glossary, diff-scope, citation pinning, deviation justifications) — process doctrine governing both v1 tail PRs and every v2 PR; codify in CLAUDE.md / PR template now.
- **[superseded-by-v2 / S]** #474 No requester signal when contacts/add orphans in pending — a wire-protocol gap (no timeout/expiry transition event); design pending-request expiry into v2's contacts lifecycle spec.
- **[superseded-by-v2 / S]** #445 claude-code-channel: MCP notifications don't wake claude --print — the single-turn exit of claude --print is an external-runtime constraint any v2 channel adapter must design around; record it in the v2 channel spec.
- **[superseded-by-v2 / S]** #444 claude-code-channel v1.1: edit_message + files — feature follow-ups; v2 channel capability inputs (protocol needs an edit-message RPC first).
- **[superseded-by-v2 / S]** #443 claude-code-channel: no fanout/broadcast for STOP-style flows — v2 protocol/channel capability input (group-conversation broadcast).
- **[superseded-by-v2 / S]** #442 claude-code-channel: outbound reactive-only, cannot initiate — v2 channel capability input (first-message initiation).
- **[close-obsolete / S]** #402 Mandatory lint gates, JSON-RPC cleanup, Effect RPC descriptors — all 12 sub-issues (#403–#412) are CLOSED and the descriptor/JSON-RPC work has landed; close the epic as complete.
- **[superseded-by-v2 / S]** #365 [spec] local-only safety guardrails — propagation-experiment guardrail spec; if the benchmark ever runs it runs against v2's protocol surface; fold into a v2-era experiment charter.
- **[superseded-by-v2 / S]** #364 [spec] propagation trace schema and metrics — same cluster as #365; trace-schema requirements become v2 observability/experiment inputs.
- **[superseded-by-v2 / S]** #363 [architect] local propagation harness architecture — targets v1 runtimes fleet surface; re-architect against v2 if the experiment survives prioritization.
- **[superseded-by-v2 / S]** #362 [spec] local contact matrix + owned-agent bootstrap — topology-as-data bootstrap is a v2 fleet/testing input.
- **[superseded-by-v2 / S]** #360 experiment: propagation benchmark infrastructure (parent epic) — dormant since 2026-05-02; keep as a v2-era research charter, not v1 work.
- **[superseded-by-v2 / S]** #349 EPIC: single-user mode default, multi-user opt-in — identity/ownership model decision (nullable owner_user_id, SYSTEM_USER_ID, webhook providers); v2 must decide this at spec time rather than inherit v1's posture.
- **[superseded-by-v2 / S]** #338 Deferred lifecycle conformance: reconnect/replay — conformance contract stub; becomes part of the v2 conformance spec (v2 must pass the lifecycle suite).
- **[superseded-by-v2 / S]** #336 Deferred lifecycle conformance: invites — same: v2 conformance spec input.
- **[superseded-by-v2 / S]** #335 Deferred lifecycle conformance: contacts — same: v2 conformance spec input.
- **[superseded-by-v2 / S]** #334 [orchestrate] Expand lifecycle conformance coverage — parent epic for #335/#336/#338; convert to the v2 conformance charter.
- **[superseded-by-v2 / S]** #282 bug(openclaw): stale cross-conversation commands leak into context — dormant since April; the fix is a context-assembly redesign (conversation-scoped isolation) that belongs in v2's runtime layer as a requirement.
- **[superseded-by-v2 / S]** #280 bug(app-sdk): channel-plugin in-flight/drain status — actually an SDK surface request, not a bug; drain/quiesce status is a v2 app-SDK requirement (phase-advance gating for apps like arena).
- **[superseded-by-v2 / S]** #279 bug(openclaw): persist per-agent context and send-result traces — structured per-turn trace persistence is a v2 runtime observability requirement.
- **[fix-in-v1 / M]** #277 server-core zero-env boot regression vs quickstart promise — the published npm package is the installable product until v2 ships; re-verify the four symptoms (port, /health path, owner_user_id RPC failure, CORS) against the current release given recent quickstart commits (aec00a46), fix what remains, then close.
- **[superseded-by-v2 / S]** #247 Epic: offline-subscriber buffering + C2 store-and-replay — the server feature does not exist (verified empirically in PR #187); store-and-replay is a v2 protocol requirement with the C2 conformance slot reserved.
- **[superseded-by-v2 / S]** #246 Epic: BackpressurePolicy + D2 conformance — zero grep matches in packages/, D2 shipped as a tombstone; design BackpressurePolicy into v2 and un-tombstone D2 there.
- **[superseded-by-v2 / S]** #245 Epic: humanContact + ServerRestart + child-process fixture (E1 SIGKILL) — humanContact/ServerRestart do not exist and the in-process test server precludes SIGKILL; all three are v2 features, restoring full E1 afterward.
- **[superseded-by-v2 / S]** #198 Runtimes own canonical skill workspace paths — skill discovery as a runtime contract (not downstream prompt convention) is a v2 runtime-SDK design input.
- **[fix-in-v1 / S]** #191 [verify] live arena operator cleanup across repos — the verify tail of #190; execute alongside it (cross-repo verify receipt), or close together if #190 is descoped.
- **[fix-in-v1 / M]** #190 Live arena operator cleanup after eval merge — `pnpm start:real` is the live dogfood loop that keeps validating v1 (and eventually v2) throughout the rewrite; interruption-safe fleet launch in @moltzap/runtimes bites every run and is worth fixing now.
- **[superseded-by-v2 / S]** #164 [spec] Effect cleanup: exact pinning + upstream config/logging/fiber tracing — the spec describes what v2 should be natively (one pinned Effect surface, reusable config/logging, fiber-aware tracing as P0); exact-pin work is already in motion (scripts/prepare-effect.sh), pull that ratchet forward if incomplete and fold the rest into v2.
- **[superseded-by-v2 / S]** #77 Rebalance test pyramid to Layer unit tests — the target profile (~200 fast Layer-injected unit tests + thin real-wire integration tier) is v2 testing doctrine from day one; do not convert v1's 79 integration tests.
- **[superseded-by-v2 / S]** #52 Should channel plugins be tamper proof — the agent-vs-plugin-code trust boundary is a v2 security-model spec question.

**Notes:** The area brief listed three categories for issues, but two issues (#733, #482) are pure enforcement/process ratchets that benefit v1 tail work and the v2 branch equally, so they carry tighten-now per the audit's global taxonomy; if the consumer requires strictly three buckets, both collapse into fix-in-v1. Cross-cutting recommendation: before the v2 branch scaffolds, do one tracker pass that (a) closes #402, (b) adds a `v2-input` label to the 35 superseded issues so the v2 spec author has a queryable corpus, and (c) posts a decision comment on #745 stating which roadmap steps (if any) still execute on the v1 branch — #745 is the one artifact where v1 execution and v2 planning are actively entangled (it currently sequences work on the pre-merge #722 branch). #277 is the only fix-in-v1 item needing re-verification before work starts; its four symptoms were reported against server-core 2026.425.4 and recent quickstart hardening commits may have already addressed a subset. Key sources: `gh issue view` bodies for all 30 non-obvious issues; sub-issue closure check for #402 (#403–#412 all CLOSED); config surface spot-check at packages/server/src/config.ts and standalone.ts for the #277 devMode/port defaults.

## architecture enforcement ("arch fully tight")

**Current state:** CI-blocking (ci.yml runs `pnpm lint`): (1) scripts/check-architecture-boundaries.js — bans `export * from` in all packages/**/*.ts, forces descriptor construction imports (defineRpc, defineNotification, decodeRpcResult, effectiveErrorClasses, jsonRpcMethod) through #transport/descriptor, bans 17 hardcoded service Tags (DbTag…TaskServiceTag) from being imported via #core, and freezes the package.json exports maps of protocol (11 subpaths) and server ([".", "./test-utils"]) — regex-based, 514 files scanned, currently green. (2) agent-code-guard-knip --treat-config-hints-as-errors over knip.json's 7 workspaces + root. (3) Per-package eslint via eslint.shared.mjs → packageEslintConfig: eslint-plugin-agent-code-guard@0.0.14 strict config (48 rules — Effect hygiene, casts, raw SQL, spans, test integrity; zero import-boundary rules), max-lines (default 1050; raised to 1100 in packages/client/eslint.config.mjs, 1200 in protocol + openclaw-channel), max-non-trivial-classes-per-file with a 12-entry TAG_CLASS_FACTORIES exemption, eslint-comments/require-description (every disable needs a reason), plus server-only cursorOpacityGuard (no-restricted-syntax on atob/base64url outside db/list-cursor.ts). (4) oxlint root pass with only 4 rules (no-explicit-any, no-unused-vars, no-console, eqeqeq). (5) tsc: tsconfig.base.json strict + noUncheckedIndexedAccess + noImplicitOverride + noFallthroughCasesInSwitch + verbatimModuleSyntax + isolatedModules; all 7 packages extend it; exactOptionalPropertyTypes explicitly false. Layer enforcement as configured but NOT running: eslint.shared.mjs → architectureSettings (publicTypePackages 6-entry allowlist, allowedTestPublicSubpaths 4-entry allowlist) and packages/protocol/eslint.config.mjs → architecture.layers (engine→app→task→network→identity→transport) are consumed only by @safer-by-default/architecture-lsp's LSP/check.js per the inline comment — that tool is absent from devDependencies, pnpm-lock.yaml, node_modules, and CI, so no-upward-layer-import / no-cross-domain-sibling-import never execute. Type-level layering: packages/server/src/moltzap/layer-tags.ts declares TransportTags⊂IdentityTags⊂NetworkTags⊂TaskTags⊂AppTags, but only AppTags is exported (via #moltzap) and consumed once — http/routes.ts bounds the WS handler R-channel with Exclude<AppTags, ConnectionTag>; the four lower unions have zero consumers, so per-layer discipline is a single global bound. Pre-commit only (bypassable, not CI): .husky/pre-commit runs scripts/sloppy-code-guard.sh (grep-based duplicate of many agent-code-guard rules + raw-rpc-method-string via scripts/check-rpc-method-strings.js). Unwired anywhere: scripts/check-no-upward-imports.sh (fails today: 110 cross-layer upward relative imports vs its stale allowlist). No Nx module boundaries: no @nx/eslint-plugin, no enforce-module-boundaries, no project tags in nx.json; the package DAG (protocol ← server/client ← channels ← runtimes; client→server-core is devDependency-only for integration tests) rests on package.json workspace deps + tsconfig.json project references + exports-map sealing (server root barrel is `export {}`). No named ACG exists: case-insensitive "acg" matches only pnpm-lock.yaml, .repos/effect vendor code, and .nx cache.

**Gaps:**
- Layer-import enforcement is dead config: architecture.layers (packages/protocol/eslint.config.mjs) and publicTypePackages/allowedTestPublicSubpaths (eslint.shared.mjs → architectureSettings) target @safer-by-default/architecture-lsp, which is not installed, not in pnpm-lock.yaml, and not invoked in ci.yml — no-upward-layer-import and no-cross-domain-sibling-import have never run in CI
- Protocol layers declaration drifted from the tree: it names folders engine/ and app/ which do not exist under packages/protocol/src, and omits real folders conversation/, message/, socket/, rpc, testing — even if the analyzer were installed it would miss half the package
- Server per-layer type enforcement is fiction: layer-tags.ts → TransportTags/IdentityTags/NetworkTags/TaskTags have zero consumers; only AppTags bounds handlers (http/routes.ts), so a transport-tier handler can pull task-tier services; server CLAUDE.md glossary ('Layer-tag hierarchy — TypeScript-enforced') overclaims
- layer-tags.ts JSDoc references dead symbols (defineMiddlewareMethod/defineUnauthMethod exist nowhere else in the tree) and a maintenance contract pointing at 'architectureOptions.layers in the root eslint.config.js' — no such file or option exists; the two-source-of-truth contract has no second source
- No package-level dependency-graph gate: no Nx enforce-module-boundaries/tags, so nothing but convention stops e.g. a channel adding a runtime dep on @moltzap/server-core or protocol importing upward
- scripts/check-no-upward-imports.sh is wired to nothing (not package.json, CI, or husky), currently FAILS with 110 violations, and its IGNORE_PATTERNS allowlist is stale (patterns kept for directories its own comments say were deleted); the promised follow-up eslint hard-fail rule never landed
- sloppy-code-guard.sh runs only in the skippable pre-commit hook, not CI; its raw-rpc-method-string check is a silent no-op because check-rpc-method-strings.js scans packages/protocol/src/{network,task,app}/methods/ — none of those directories exist; it also scans packages/evals/ which contains no source
- check-architecture-boundaries.js freezes only protocol + server export maps — client (6 subpaths incl ./channel-base, ./auth, ./pagination, ./notification), the three channels, and runtimes are unchecked; its 17-name #core tag-ban list is hardcoded and must be hand-synced with layer-tags.ts
- allowedTestPublicSubpaths allowlist carries stale entries: './test' and './test-support' match no package's exports map
- oxlint.json: only 4 rules enabled, and ignorePatterns lists 5 nonexistent directories (openclaw/, moltbook-api/, moltbook-frontend/, A2A/, awesome-molt-ecosystem/) — copied from another repo
- tsconfig.base.json opts out of exactOptionalPropertyTypes; noUnusedLocals/noImplicitReturns/noPropertyAccessFromIndexSignature absent (unused-vars partially covered by oxlint)
- Positive baseline worth preserving: 0 @ts-expect-error/@ts-ignore outside *.types-check.ts canaries, 0 'as any' in production source, all 23 eslint-disable sites carry enforced justifications

**Actions:**
- **[tighten-now / M]** Make layer-import enforcement real in CI: either add @safer-by-default/architecture-lsp check.js to devDependencies and the root `pnpm lint` chain, or port the layer DAG to plain eslint no-restricted-imports/import-x rules per package — until one of these lands, delete or comment-mark the architecture.layers config as aspirational
- **[tighten-now / S]** Correct packages/protocol/eslint.config.mjs architecture.layers to the actual folder set (drop engine/app; add conversation, message, socket, rpc, testing or document why they are exempt) so the config is truthful the day an analyzer runs
- **[tighten-now / M]** Adopt a package-level dependency-graph gate (Nx enforce-module-boundaries with project tags protocol < server-core/client < channels < runtimes, or a small script asserting the workspace-dep DAG) so the inter-package layering is CI-enforced, not conventional
- **[fix-in-v1 / S]** Rewrite packages/server/src/moltzap/layer-tags.ts JSDoc for a cold reader: remove dead defineMiddlewareMethod/defineUnauthMethod and root-eslint.config.js references, state that AppTags is the only applied bound, and fix the server CLAUDE.md 'Layer-tag hierarchy' glossary overclaim
- **[superseded-by-v2 / M]** Carry per-tier R-channel enforcement into the v2 spec: each handler tier bounded by its own union (TransportTags…AppTags) at the registration site, plus per-package layers declared for server and client from day one — do not retrofit v1 handler plumbing
- **[close-obsolete / S]** Delete scripts/check-no-upward-imports.sh (unwired, failing at 110, stale allowlist); fold its surviving intent (workspace-name imports for cross-layer paths) into the v2 import-boundary rules
- **[tighten-now / S]** Fix or delete the silent no-op scripts/check-rpc-method-strings.js (its methodsDirs point at nonexistent packages/protocol/src/{network,task,app}/methods/); if kept, derive dirs from the real descriptor locations and add a self-test that methodByName is non-empty
- **[tighten-now / M]** Promote the non-redundant subset of scripts/sloppy-code-guard.sh into CI (or delete checks now covered by agent-code-guard eslint rules) so guards stop living only in the bypassable pre-commit hook; drop the packages/evals scan
- **[tighten-now / S]** Extend check-architecture-boundaries.js assertExportMap to client, the three channels, and runtimes, and generate the #core tag-ban list from layer-tags.ts imports instead of a hardcoded 17-name regex
- **[tighten-now / S]** Burn down stale allowlists: remove './test' and './test-support' from allowedTestPublicSubpaths in eslint.shared.mjs; remove the 5 dead ignorePatterns dirs from oxlint.json
- **[tighten-now / S]** Decide oxlint's role: either expand its rule set to earn its CI slot or drop it and let eslint own linting — the current 4-rule config plus copied-in ignores is split-brain
- **[superseded-by-v2 / S]** Set exactOptionalPropertyTypes: true (plus noImplicitReturns/noPropertyAccessFromIndexSignature evaluation) as the v2 tsconfig baseline; flipping it across v1 is churn with little payoff given the rewrite

**Notes:** Key files: /Users/tapanc/moltzap/eslint.shared.mjs (architectureSettings, packageEslintConfig), /Users/tapanc/moltzap/packages/protocol/eslint.config.mjs (layers), /Users/tapanc/moltzap/scripts/check-architecture-boundaries.js (the only CI-enforced boundary check), /Users/tapanc/moltzap/scripts/check-no-upward-imports.sh (unwired), /Users/tapanc/moltzap/scripts/sloppy-code-guard.sh + check-rpc-method-strings.js (pre-commit only; latter no-op), /Users/tapanc/moltzap/packages/server/src/moltzap/layer-tags.ts (AppTags consumed once in packages/server/src/http/routes.ts), /Users/tapanc/moltzap/.github/workflows/ci.yml, /Users/tapanc/moltzap/nx.json (no boundary plugin), /Users/tapanc/moltzap/tsconfig.base.json. No named 'ACG' exists anywhere in the repo — the 'acg' search matches only pnpm-lock.yaml, .repos/effect vendor sources, and .nx cache artifacts; the de-facto architecture guard is check-architecture-boundaries.js plus the never-executed architecture-lsp settings. Escape-hatch hygiene where rules DO run is genuinely tight (0 as-any, 0 non-canary ts-expect-error, all 23 eslint-disables justified via eslint-comments/require-description), so the gap is coverage/wiring, not discipline. For v2: the single highest-leverage ratchet is making layer + package-DAG checks execute in CI before v2 scaffolding starts, since v2 will inherit whatever enforcement rig exists at branch point.

## docs infrastructure (Mintlify site, typedoc/MODULE.md pipeline, constants baking, drift gates)

**Current state:** Mintlify site under docs/ driven by docs/docs.json (3 tabs: Documentation, Protocol Reference, CLI). Four generated surfaces, all committed to git: (1) docs/protocol/methods/*.mdx + notifications/*.mdx rendered from RPC descriptors + JSDoc by packages/protocol/scripts/generate-docs.ts (helpers in scripts/docs/{render,rpc-jsdoc,schema}.ts; prunes stale pages via deleteStaleGeneratedPages, but only LOGS nav entries — docs.json method/notification groups are hand-pasted); (2) per-folder MODULE.md + MDX twins under docs/modules/ from the TypeDoc JSON cache (root typedoc.json, entryPointStrategy=packages over 7 packages → node_modules/.cache/typedoc.json; packages/protocol/scripts/generate-modules.ts + scripts/docs/modules.ts → generateModuleDocs; opt-in = leading file JSDoc on index.ts; orphan pruning via pruneOrphans; generator-owned nav docs/modules/_nav.json referenced from docs.json via $ref written by scripts/docs/mintlify-nav.ts → writeModulesNav; server dir is slug-mapped by packageSlugFor "server"→"server-core"); (3) CLI docs captured from `moltzap --help` by packages/client/scripts/generate-cli-docs.ts → docs/cli/reference.mdx + snippets (cli-commands-table, cli-global-flags, ws-connect-example); (4) scripts/generate-constants-snippets.ts reads canonical literals via TS AST (PROTOCOL_VERSION from packages/protocol/package.json, DEFAULT_APP_ID, DEFAULT_SERVER_PORT, API_KEY_PREFIX, QUICKSTART_PORT from scripts/quickstart.sh) → docs/snippets/constants/values.{json,mdx} and bakes literals into files carrying `{/* @bake-constants: ... */}` markers. Sync enforcement: CI (.github/workflows/ci.yml) runs `docs:check:drift` = `docs:generate && git diff --exit-code docs/ packages/**/MODULE.md README.md` plus scripts/check-no-hardcoded-constants.ts (literal scan with allowlists, bake-marker exemptions, version-shaped catch-all, STALE_PORT_3100 hard reject) and scripts/check-doc-imports-resolve.ts (validates `from "@moltzap/*"` imports in docs against exports maps + named bindings via TS AST); `docs:check:gates-test` (scripts/__tests__/gates.test.ts) self-tests the gates by planting regressions in a temp mirror. .husky/pre-commit runs docs:generate + diff + both gates on every commit; publish.yml regenerates and commits docs after version bumps. Flow coverage (scripts/docs/flow-coverage.ts → computeFlowCoverage) reports behavioral exports lacking JSDoc summary/mermaid but always exits 0. docs:check (mint broken-links) and docs:check:mermaid (packages/protocol/scripts/check-mermaid.ts, pipes fenced blocks through mmdc) exist but run in neither CI nor pre-commit.

**Gaps:**
- Hand-written prose is entirely ungated and has confirmed stale claims: docs/introduction.mdx (3x), docs/architecture.mdx, docs/development/contributing.mdx, and README.md:193/229 all say the protocol is 'TypeBox schemas' with 'AJV validators' / 'Static<typeof Schema>' — the codebase is Effect Schema (packages/protocol/src uses Schema.Struct; contributing.mdx even contradicts itself in its own Code style section)
- docs/architecture.mdx sequence diagram shows `HelloOk {agentId, protocolVersion, policy}` but HelloOkSchema in packages/protocol/src/network/connect.ts is an empty Struct (docs/protocol/overview.mdx has the correct wording); its package dependency graph also omits @moltzap/runtimes and @moltzap/claude-code-channel
- docs/development/contributing.mdx claims generation reads 'one methods.ts per layer: identity/, network/, task/, app/' — no methods.ts exists anywhere and app/ is not a layer
- docs/development/protocol-organization.mdx is a 721-line hand-maintained 'source of truth' file tree + wire-surface table that already drifted (missing packages/protocol/src/transport/descriptor.ts, missing rpc/ vs rpc.ts split) and contradicts the workspace CLAUDE.md rule that structure docs live in JSDoc/MODULE.md; no gate compares its wire tables to the RPC catalog
- docs:check:drift uses `git diff --exit-code`, which ignores untracked files: a NEW generated page (new RPC method) passes the gate; combined with hand-pasted docs.json nav for methods/notifications (generate-docs.ts only logs 'Method nav entries'), a new RPC page can silently never appear in the published nav
- No CI check that docs.json/_nav.json entries and docs/**.mdx files are in bijection (verified currently consistent by ad hoc script, but nothing enforces it), and `mint broken-links` (docs:check) is not in CI
- docs:check:mermaid is not wired into CI or pre-commit despite workspace CLAUDE.md's list of GitHub-breaking mermaid patterns — invalid diagrams can ship
- Module-docs coverage is silently partial: nanoclaw-channel and openclaw-channel have no src/index.ts, so discoverFolders skips them without warning (the loud MissingJsDoc path only fires when index.ts exists); two shipped packages have zero MODULE.md/module pages; their per-package typedoc.json `outputs` (node_modules/.cache/typedoc-pkg.json) is consumed by nothing
- Flow-coverage report (computeFlowCoverage) is exit-0 informational — no ratchet stops JSDoc/diagram coverage from regressing
- Generated MDX export links use GitHub blob/main#L<line> permalinks (SOURCE_LINK_BASE in modules.ts): line anchors rot between regen and merge, and any line shift in a documented file forces regen churn in unrelated commits via the pre-commit diff gate
- packageSlugFor hardcodes the packages/server dir → 'server-core' slug rename — naming split between directory, npm name, and docs slug
- Cold-reader-rule violations in the pipeline's own comments: generate-modules.ts header narrates 'during the pilot rollout it ships as a standalone script' (it is wired into docs:generate), generate-docs.ts @file says 'from TypeBox schemas', and .husky/pre-commit's remediation message says only 'git add docs/protocol' though regen also touches docs/modules, MODULE.md, README.md
- No normative spec surface exists: protocol semantics are split across hand-written docs/protocol/{overview,transport,frames,errors}.mdx, generated method pages, and conformance suites in packages/protocol/src/testing/conformance — no stable requirement IDs, no RFC-2119 conventions, no explicit heading anchors (Mintlify derives anchors from heading text, so renames silently break deep links), no docs versioning (Mintlify `versions` unused; docs track main only while PROTOCOL_VERSION is CalVer-released)

**Actions:**
- **[fix-in-v1 / S]** Correct stale prose: TypeBox/AJV/Static claims in docs/introduction.mdx, docs/architecture.mdx, docs/development/contributing.mdx (incl. nonexistent methods.ts/app-layer), README.md:193/229; fix architecture.mdx HelloOk payload diagram and add runtimes/claude-code-channel to its dependency graph; fix generate-docs.ts @file docstring and generate-modules.ts 'pilot rollout' narration; update .husky/pre-commit remediation message
- **[tighten-now / S]** Wire `pnpm docs:check` (mint broken-links) and `pnpm docs:check:mermaid` into ci.yml alongside docs:check:drift
- **[tighten-now / S]** Close the drift-gate blind spot: replace `git diff --exit-code` in docs:check:drift with a `git status --porcelain`-based check so new untracked generated files fail, and add a nav-consistency gate asserting docs.json+_nav.json entries ↔ docs/**.mdx files in both directions (excluding snippets/)
- **[tighten-now / M]** Make generate-docs.ts own the methods/notifications nav slice as a second generated $ref file (mirroring writeModulesNav/_nav.json) instead of logging entries for manual docs.json paste
- **[fix-in-v1 / S]** Delete or drastically shrink the hand-maintained file tree and wire-surface tables in docs/development/protocol-organization.mdx (already drifted); keep only the layering/naming RULES, and point structure questions at generated MODULE pages
- **[fix-in-v1 / S]** Make module-docs coverage skips loud: emit a warning (or gate) when a typedoc entrypoint package contributes zero MODULE pages (nanoclaw-channel, openclaw-channel today), and delete the unconsumed per-package typedoc.json `outputs` blocks
- **[close-obsolete / S]** Remove the orphaned node_modules/.cache/typedoc-pkg.json output config once confirmed nothing external reads it (grep found no consumers)
- **[tighten-now / M]** Ratchet flow-coverage from exit-0 report to a baseline-count gate (fail on regression, allow explicit baseline bumps)
- **[superseded-by-v2 / L]** v2 spec input — design docs/spec/ as a generated-from-registry chapter set: machine-readable requirement registry (one record per normative requirement with stable ID like SPEC-MSG-004) rendered to MDX with EXPLICIT heading anchors, RFC-2119 term linting, a traceability gate mapping requirement IDs to conformance tests in src/testing/conformance (annotate tests with IDs, fail on unreferenced requirements), and per-PROTOCOL_VERSION spec snapshots via Mintlify `versions` or frozen copies committed by publish.yml, plus a redirects map gate so renamed anchors never 404
- **[superseded-by-v2 / M]** v2 spec input — replace blob/main#L<line> permalinks in generated module MDX with symbol-stable links (e.g. GitHub code-search links or release-tag permalinks) to stop line-shift regen churn, matching the repo's own symbol-not-line citation rule
- **[superseded-by-v2 / S]** v2 spec input — unify package directory / npm name / docs slug (packages/server vs @moltzap/server-core vs modules/server-core) so packageSlugFor's hardcoded rename disappears
- **[superseded-by-v2 / M]** v2 spec input — carry forward the proven ratchet patterns as-is: AST-sourced constants baking (generate-constants-snippets.ts), gate self-testing (gates.test.ts), doc-import resolution (check-doc-imports-resolve.ts), and opt-in MODULE.md generation; these are the strongest parts of v1 docs infra and should be scaffolded into v2 from day one

**Notes:** The generated surfaces are well-defended (regen+diff in CI and pre-commit, constants baked from AST, gates that test themselves) — drift lives almost exclusively in hand-written prose, which has no gate at all; every confirmed stale claim (TypeBox/AJV, HelloOk payload, methods.ts) is in that layer. The asymmetry to fix cheaply: modules nav is generator-owned via $ref while methods/notifications nav is hand-pasted, and `git diff --exit-code` cannot see new untracked generated files. For hosting a normative spec: Mintlify gives no anchor stability (anchors derive from heading text) and versioning is unused, so a spec chapter set needs explicit IDs-as-anchors, a requirement registry as the generation source (same pattern as the constants pipeline), requirement↔conformance-test traceability (the conformance suites in packages/protocol/src/testing/conformance are the natural join key), and per-release snapshots hooked into publish.yml where docs regeneration already runs. Audit was read-only; docs:generate/typedoc were not executed (typedoc cache at node_modules/.cache/typedoc.json dated today from a prior run). Note plans/social-harness/ (untracked) contains v2-planning material; docs/development/local-setup.mdx has an unrelated staged edit in this worktree.

## conformance coverage (packages/protocol/src/testing/conformance vs packages/protocol/src/socket/catalog)

**Current state:** The conformance framework (packages/protocol/src/testing/conformance) is Effect-native: `_shared/suite.ts → runConformanceSuite` is the single consumer entry, `_shared/registry.ts → registerProperty/PropertyFailure` gives typed failures, `_shared/runner.ts → acquireRunContext` gates tiers A–E (D = Toxiproxy adversity), and `_shared/coverage-policy.ts → isAllowedCoverageGap` whitelists known gaps. ~33 registered properties across 5 suites: transport (4 adversity props), identity (1: authority-positive, which only calls agent/task/list), network (1: presence-subscribe-after-connect), task (7 lifecycle/delivery props + 4 conversation-family per-method props), app (16 dispatch/lease/callback props incl. idempotence over AgentsList+TaskList). Against the full catalog (`socket/catalog/index.ts`: 13 agent-callable + 6 app-callable + 3 app-callback RPCs + 14 notifications, plus HTTP `agent/identity/register`), well covered: the whole dispatch/lease domain (agent/dispatch/request, app/dispatch/lease/get, app/dispatch/authorize incl. timeout fail-closed, all 3 dispatch notifications), message delivery (agent/message/send + agent/message/received via fan-out/store-and-replay/payload-opacity/task-boundary-isolation), task lifecycle (request/list/leave/update, app/task/create callback, all 3 task notifications), conversation lifecycle (create/update/list + all 5 conversation notifications), agent presence subscribe; both connects exercised implicitly by every property's handshake (`_shared/driver/test-client.ts`, `_shared/test-app.ts`). Consumers: ONLY `packages/server/src/__tests__/conformance/suite.test.ts` actually runs the suite. The client and all three channel packages (openclaw, nanoclaw, claude-code) have `test:conformance` scripts whose vitest configs carry `passWithNoTests: true` against a `src/__tests__/conformance/**` glob that matches ZERO files — client-side conformance was retired in commit 38dcabe8 ("Stabilize protocol socket cutover"), so the four channel/client steps in .github/workflows/conformance.yml are vacuous green no-ops. Conformance is not an nx target; ci.yml's `nx affected` never runs it — only the path-filtered conformance.yml workflow does, and only the server step is real.

**Gaps:**
- Entire contacts surface has zero conformance coverage: agent/identity/contacts/add, contacts/accept, contacts/list plus the contact-requested and contact-accepted notifications (5 of 37 catalog items) — no conformance file imports ContactsAdd/ContactsAccept/ContactsList or either contact notification definition
- agent/message/list (and its pagination cursor contract) has zero coverage; agent/identity/agents/list is only touched by the idempotence replay-equality check, no semantic assertions
- app/network/presence/subscribe has zero coverage (only the agent-side presence subscribe is tested)
- No negative-path authority coverage anywhere: identity/authority-negative.ts was deleted in commit 38dcabe8, leaving only authority-positive; no property asserts that a wrong-principal or unauthenticated call yields a typed requirement failure, despite every descriptor carrying `requires` metadata that could drive a table-driven check
- app/message/authorize callback is only exercised as fixture plumbing (test-app.ts scripted handler); no property asserts its verdict or timeout fail-closed semantics, unlike app/dispatch/authorize which has both
- The 'schema-conformance' PropertyCategory is declared in registry.ts but zero properties register under it; testing/arbitraries (schema-arbitrary.ts) is exported from the testing barrel but wired into no wire-schema round-trip property
- boundary/app-disconnect-fail-policy permanently returns PropertyUnavailable (its JSDoc says the fixture cannot assemble the app-topology dispatch precondition) and sits on BASE_ALLOWED_SERVER_COVERAGE_GAPS — the fail-CLOSED disconnect contract is documented but never actually tested
- adversity/reset-peer-recovery is allowed to silently skip even with Toxiproxy present (allowed gap 'reset_peer toxic did not close')
- Third-party/channel conformance is dead: the four CI steps (client, openclaw, nanoclaw, claude-code) in conformance.yml run vitest configs with passWithNoTests:true over empty globs — a channel can drift from the protocol with a green Conformance check; there is no client-half harness (fake server certifying a channel implementation) at all
- Doc drift: packages/claude-code-channel/CLAUDE.md (test table row for src/__tests__/conformance/suite.test.ts) and packages/nanoclaw-channel/CLAUDE.md (project-structure entry for src/__tests__/conformance/) document directories deleted in 38dcabe8
- No catalog-vs-coverage ratchet: nothing fails when a new descriptor is added to socket/catalog without a conformance property, so coverage can only decay silently
- Conformance runs outside nx (pnpm -F in a path-filtered workflow), so no caching/affected semantics and no local single-command story beyond the server package

**Actions:**
- **[tighten-now / S]** Delete the four vacuous test:conformance CI steps and their passWithNoTests:true vitest configs (client, openclaw-channel, nanoclaw-channel, claude-code-channel), or repopulate them; a permanently-green empty gate is worse than no gate
- **[tighten-now / M]** Add a catalog-driven coverage meta-check: iterate serverInboundMethods + notificationDefinitions + appCallbackMethods from socket/catalog and fail when a descriptor has neither a registered conformance property nor an explicit allowed-gap entry — this is the ratchet that keeps both v1 and the v2 rewrite honest
- **[fix-in-v1 / M]** Restore negative-path authority as one table-driven property generated from each descriptor's `requires` metadata (wrong principal / unauthenticated → typed requirement failure), replacing the deleted authority-negative.ts
- **[fix-in-v1 / M]** Add app/message/authorize verdict + timeout fail-closed properties mirroring the existing dispatch-authorize pair — this is a live security gate in the v1 server that currently ships untested at the conformance level
- **[fix-in-v1 / M]** Wire the app-topology fixture (manifest-declared conversation bootstrap) so boundary/app-disconnect-fail-policy actually runs, and remove it from BASE_ALLOWED_SERVER_COVERAGE_GAPS
- **[fix-in-v1 / S]** Investigate and fix the reset_peer toxic flake so adversity/reset-peer-recovery cannot silently skip when Toxiproxy is present
- **[close-obsolete / S]** Update packages/claude-code-channel/CLAUDE.md and packages/nanoclaw-channel/CLAUDE.md to stop documenting the deleted src/__tests__/conformance/ directories
- **[superseded-by-v2 / L]** Write per-method conformance properties for the uncovered surface — contacts add/accept/list + contact-requested/contact-accepted notifications, agent/message/list pagination, app/network/presence/subscribe, semantic agents/list — as v2 conformance-spec input rather than v1 backfill; the v2 catalog should launch with a property per descriptor
- **[superseded-by-v2 / M]** Wire testing/arbitraries into schema-conformance round-trip properties (params/result encode/decode, excess-key rejection) — design this into the v2 suite where descriptors and arbitraries can be co-generated
- **[superseded-by-v2 / L]** Design and build the client-half conformance harness (fake server driving a channel-under-test: handshake, notification handling, callback verdicts) so third-party channels can actually certify — the retired v1 client-side suite becomes a v2 requirement, not a v1 restoration
- **[tighten-now / S]** Move test:conformance under nx targets so affected/caching semantics apply and the path filter list in conformance.yml stops being hand-maintained

**Notes:** Key files: packages/protocol/src/testing/conformance/_shared/{suite,runner,registry,coverage-policy}.ts (framework), packages/protocol/src/socket/catalog/index.ts (full surface: 22 socket RPCs + 14 notifications + HTTP register), packages/server/src/__tests__/conformance/suite.test.ts (the only real consumer), .github/workflows/conformance.yml (five steps, four vacuous), packages/{client,openclaw-channel,nanoclaw-channel,claude-code-channel}/vitest.conformance.config.ts (all identical: passWithNoTests:true, comment 'Client-side conformance was retired with the typed Effect RPC transport'). Coverage headline: roughly 26 of 37 catalog items are exercised by at least one property; the contacts domain, message/list, app-side presence subscribe, negative authority, message-authorize semantics, and all schema round-trip checking are the uncovered remainder. The suite architecture itself (typed PropertyFailure, allowed-gap policy, consumer-supplied realServer handle, tiered Toxiproxy adversity) is sound and worth carrying into v2 as-is; the debt is in breadth of properties and in the dead client-half, not in the framework.

## CI and release guardrails (.github/workflows, scripts/, husky, nx targets, publish path)

**Current state:** PR gates today (3 workflows, all in /Users/tapanc/moltzap/.github/workflows/): (1) ci.yml job `ci` — pnpm install, `test:effect-source-bootstrap` (node --test scripts/prepare-effect.test.mjs), full `pnpm build`, `pnpm typecheck` (alias of the same `nx run-many -t build`, cache hit), `pnpm lint` (scripts/check-architecture-boundaries.js + agent-code-guard-knip + root eslint + per-package eslint + oxlint), `format:check` (oxfmt), `docs:check:drift` (docs:generate + `git diff --exit-code docs/ ...` + check-no-hardcoded-constants.ts + check-doc-imports-resolve.ts), and `docs:check:gates-test` (scripts/__tests__/gates.test.ts — meta-tests that plant regressions and assert the doc gates catch them, pin the publish.yml manifest→build→docs→stage ordering via string indexOf in testPublishWorkflowVersionFlow, and pin the Node 22 floor in testNodeVersionFloorConsistency). (2) ci.yml job `test` — `nx affected -t typecheck:tests test test:integration` with nx-set-shas. (3) conformance.yml — path-filtered; Toxiproxy via docker compose, then `test:conformance` for server-core, client, openclaw-, nanoclaw-, claude-code-channel; failure artifacts uploaded. Local-only gates: .husky/pre-commit runs oxfmt (writes), docs:generate, a `git diff --quiet` cleanliness check, both doc constant/import gates, `pnpm check`, full build, typecheck:tests, and scripts/sloppy-code-guard.sh (grep-based type-safety/Effect-hygiene/test-integrity rules with pragma opt-outs); no pre-push hook. Release path: publish.yml fires on every push to main (skipping its own `chore(release):` commits), detects changed packages by diffing since the last release commit over a hardcoded list (protocol server client openclaw-channel), computes date-based versions via scripts/compute-next-version.sh (npm view + YYYY.MDD.N), writes package.json versions, rebuilds, regenerates docs, commits `chore(release): ...` straight to main with a GitHub App token, then `pnpm pack` + `npm publish --provenance` (OIDC, Node 24) for any local version not yet on npm — self-healing across failed runs. Effect source bootstrap: scripts/prepare-effect.mjs (pinned effect@3.22.0 clone with checkout verification + unit tests; replaces the unpinned prepare-effect.sh still present at HEAD — the swap is uncommitted work on branch agent/fix-publish-version-discovery, along with MOLTZAP_SKIP_EFFECT_SOURCE=1 additions to all three workflows). Evals: packages/evals is scenario YAML only; the harness lives in packages/runtimes/src/trace-capture-harness.ts and is run manually via the external cc-judge CLI with real LLM credentials — never in CI.

**Gaps:**
- CONFIRMED broken: packages/runtimes/src/trace-capture-harness.ts → loadClientTestModule dynamically imports packageModuleUrl("client", "dist", "test", "index.js") — packages/client has no src/test/ (only src/test-utils/), and the contract it casts to (ClientTestModule.registerAndConnect) no longer exists anywhere in packages/client (registerAndConnect now lives in packages/server/src/test-utils/helpers.ts). Broken since the socket cutover (38dcabe8); invisible to build/typecheck because it is a stringly dynamic import cast to Promise<ClientTestModule>, and no test exercises it.
- Publish is not gated on CI: publish.yml triggers on push to main in parallel with ci.yml/conformance.yml, runs pnpm build but zero test suites, and can ship a broken merge to npm before CI finishes.
- No concurrency groups in any workflow: two rapid merges to main race two publish runs (mitigated only by the npm-view skip + git push --atomic bail); PR CI runs are never cancel-in-progress.
- sloppy-code-guard.sh is enforced only in .husky/pre-commit — `git commit --no-verify` (or any web-UI merge) lands violations CI never checks.
- publish.yml hardcodes `for pkg in protocol server client openclaw-channel`: @moltzap/claude-code-channel (public, 0.1.0) and @moltzap/runtimes (public, 0.2.0) are never released by the workflow, and a workflow_dispatch `packages` input naming them is a silent no-op.
- Dependents are not republished when a workspace dep bumps: change detection is per-package-dir, and pnpm pack rewrites workspace:* to an exact version — so a protocol-only release leaves the latest published @moltzap/server-core hard-pinned to the previous protocol version.
- Not wired into CI at all: scripts/compute-next-version.test.sh (the versioning logic has tests that never run), docs:check:mermaid (despite CLAUDE.md's GitHub-mermaid strictness), docs:check (mint broken-links), and the stress conformance profile (test:conformance:stress).
- scripts/check-no-upward-imports.sh is orphaned — referenced by nothing (root package.json lint chain uses check-architecture-boundaries.js instead).
- quickstart.sh port override is broken: the generated moltzap.yaml hardcodes `port: 41973` in a quoted heredoc and the server launch passes no PORT env (packages/server/src/config.ts reads PORT ?? yaml.server.port), so the script's own suggested remedy `MOLTZAP_PORT=<other> ./scripts/quickstart.sh` health-checks a port the server never binds; the `ss` preflight silently no-ops on macOS; and the closing echo narrates 'Phase 7 cutover / Phase 9 / Phase 14' migration history, violating the repo's cold-reader rule.
- .husky/pre-commit `git diff --quiet` conflates hook-generated changes with pre-existing unstaged edits — any dirty tracked file fails the commit even when the hook changed nothing, breaking git add -p workflows; the hook also runs full build + full lint + docs:generate on every commit (minutes per commit), which pushes people toward --no-verify, the exact hole noted above.
- CI runs cold every time: NX_SKIP_REMOTE_CACHE=true in ci.yml/conformance.yml and no actions/cache for .nx — full rebuild per run despite the Railway remote cache existing for local dev.
- v2-branch coverage hole: ci.yml `push` only fires on main, and conformance push likewise — direct pushes to a long-lived v2 integration branch get no CI (PRs targeting it do run, since `pull_request` is unfiltered).
- gates.test.ts → testPublishWorkflowVersionFlow pins publish.yml behavior via raw string indexOf on the YAML — a legitimate refactor of the workflow text breaks the gate without any behavior change (acceptable ratchet, but brittle).
- compute-next-version.sh BASE discovery in publish.yml (`git log --grep="^chore(release):"`) matches any commit whose message body contains that line-start string, not only true release commits — a squashed PR quoting a release message shifts the diff base.

**Actions:**
- **[tighten-now / M]** Gate publish on green CI: convert publish.yml to trigger on workflow_run (CI completed, conclusion == success) or move to tag/release-driven publishing, and add a `concurrency: publish-main` group with cancel-in-progress: false. This is the single highest-leverage release guardrail and carries directly into v2.
- **[tighten-now / S]** Run scripts/sloppy-code-guard.sh (or its checks) as a CI step in ci.yml so pre-commit bypass cannot land violations; keep the hook as fast local feedback.
- **[tighten-now / S]** Add concurrency groups with cancel-in-progress: true to ci.yml and conformance.yml PR runs.
- **[tighten-now / S]** Wire the never-run gates into ci.yml: scripts/compute-next-version.test.sh, docs:check:mermaid, and (nightly or on docs/ changes) mint broken-links.
- **[tighten-now / S]** Extend ci.yml and conformance.yml push triggers to include the v2 integration branch (e.g. branches: [main, v2]) before v2 scaffolding starts, so direct pushes to the v2 branch are tested from day one; confirm publish.yml stays main-only.
- **[fix-in-v1 / S]** Fix publish.yml package-list drift: derive the publishable set from workspace packages with private != true (or at minimum add claude-code-channel/runtimes deliberately and make workflow_dispatch fail loudly on an unrecognized package name instead of silently no-oping).
- **[fix-in-v1 / M]** Decide and implement dependent-republish semantics: when a workspace dep (e.g. protocol) releases, either cascade-bump its dependents in the same publish run or document that exact-pinned dependents intentionally lag; today the latest published server-core silently pins a stale protocol.
- **[fix-in-v1 / M]** Fix or excise trace-capture-harness.ts → loadClientTestModule: the dist/test/index.js import path and the ClientTestModule.registerAndConnect contract are dead. Repoint to the current client surface (@moltzap/client/auth registerAgent + a connect helper) with a smoke test that actually performs the dynamic import, or delete the harness path until v2. Do not leave a silently-dead code path.
- **[fix-in-v1 / S]** quickstart.sh: pass PORT="$PORT" (or template the yaml port) when launching the server so MOLTZAP_PORT actually works; add an lsof fallback for the ss preflight on darwin; strip the 'Phase 7/9/14' migration narration per the repo cold-reader rule.
- **[fix-in-v1 / M]** Fix .husky/pre-commit cleanliness check to diff only against a pre-hook snapshot (e.g. git stash-hash comparison or `git diff --name-only` scoped to the files the hook regenerates) instead of global `git diff --quiet`, and trim the hook to affected-only targets so it stops incentivizing --no-verify.
- **[close-obsolete / S]** Delete scripts/check-no-upward-imports.sh (orphaned; check-architecture-boundaries.js is the wired equivalent).
- **[close-obsolete / S]** Delete scripts/prepare-effect.sh at HEAD once the in-flight prepare-effect.mjs swap on agent/fix-publish-version-discovery merges (the working tree already deletes it; ensure the commit lands rather than lingering).
- **[superseded-by-v2 / L]** v2 spec input: replace the hand-rolled publish.yml bash (version discovery via git log --grep, jq manifest writes, per-package npm-view skip) with a managed release tool (nx release or changesets) that owns changed-package detection, dependent bumps, tags, and GitHub Releases; keep the OIDC --provenance publishing and the self-healing 'publish anything local not on npm' property as requirements.
- **[superseded-by-v2 / L]** v2 spec input: port sloppy-code-guard.sh rules (raw-sql, promise-type, async-keyword, as-unknown-as, tag-discriminant, raw-rpc-method-string, etc.) to typed lint rules (eslint/oxlint plugin or ts-morph) from day one — the script's own header flags the bash implementation as unwieldy, its pragma parser fails open on typos, and grep rules cannot see syntax.
- **[superseded-by-v2 / L]** v2 spec input: rebuild the eval harness contract against the v2 client public surface with a CI-runnable smoke tier (fake-model or replay-based) so 'evals import real modules' is enforced by a test, not by hope; keep real-LLM cc-judge runs manual but make module loading covered.
- **[superseded-by-v2 / S]** v2 spec input: replace the string-indexOf workflow assertions in gates.test.ts (testPublishWorkflowVersionFlow) with structural checks (parse the YAML) or drop them once the release tool owns ordering guarantees.
- **[tighten-now / M]** Enable nx computation caching in CI (either the existing Railway remote cache with a CI token, or actions/cache on .nx/cache keyed by lockfile+nx.json) — cuts every gate's wall time for both v1 maintenance and the v2 branch.
- **[tighten-now / M]** Add baseline supply-chain guardrails absent today: pin third-party actions to SHAs, add dependabot/renovate config, and a CodeQL (or equivalent) workflow — none exist under .github/ besides the three workflows.

**Notes:** Verification details: client dist/test/index.js absence confirmed by listing packages/client/src (no test/ dir) and packages/client/dist; registerAndConnect greps to zero hits in packages/client/src and to packages/server/src/test-utils/helpers.ts — so the harness break is a contract break, not just a path rename; the sibling loadServerTestModule path (server/dist/test-utils/server.js) still resolves. The working tree on branch agent/fix-publish-version-discovery carries uncommitted guardrail work (prepare-effect.sh → prepare-effect.mjs, MOLTZAP_SKIP_EFFECT_SOURCE=1 in all three workflows, pnpm overrides pinning @effect/cluster|experimental|workflow) — my report describes the working-tree state; the audit's fix-in-v1 items assume that branch lands. Package publish state: protocol/server/client/openclaw-channel at 2026.717.0 (date-versioned, released by the workflow); claude-code-channel 0.1.0 and runtimes 0.2.0 are public but outside the release loop; nanoclaw-channel is private. Node split is intentional: CI on 22 (product floor), publish on 24 (npm >= 11.5 for OIDC trusted publishing). The evals-mock-model rule in sloppy-code-guard.sh scans packages/evals/ which now contains only YAML scenarios — the rule still works but its target moved to packages/runtimes, which it does not scan (minor drift worth folding into the CI-side guard port).
