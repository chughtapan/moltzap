/**
 * @file Production composition and the executable boundary for the
 * profile-slot daemon and its loopback MCP surface. That MCP edge is a
 * trusted-local runtime boundary, not the Router data plane.
 */

export {};
