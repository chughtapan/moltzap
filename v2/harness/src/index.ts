/**
 * @file Public Harness capabilities. The package owns the interpretive
 * protocol engine and its adapter-facing client boundary. The endpoint's
 * Harness subsystem is the only place that interprets message bodies.
 */

export {};
