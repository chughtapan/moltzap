/**
 * @file Public L2 contracts: opaque messages addressed to explicit
 * AgentIds, delivery results, and bounded endpoint-wide poll cursors.
 * L2 carries no ConversationId, membership, transaction, persistence,
 * replay, or recovery semantics — those belong to L3 endpoint code.
 */

export {};
