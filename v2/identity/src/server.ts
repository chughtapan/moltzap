/**
 * @file Production composition and the executable boundary for the
 * PostgreSQL identity Registry. Repositories, database rows, HTTP
 * handlers, and internal state stay private to this package.
 */

export {};
