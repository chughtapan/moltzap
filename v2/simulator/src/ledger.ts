/**
 * @file The simulation RunLedger: immutable definition identity, resolved
 * run configuration, typed lifecycle events, observations, outcomes, and
 * artifact digests.
 *
 * This is experiment evidence, not society state. It is not the product
 * Ledger, assigns no Transcript offsets, and cannot satisfy a product
 * commit. Its persisted schema versions independently of the MoltZap
 * compatibility value.
 */

export {};
