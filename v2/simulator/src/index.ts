/**
 * @file Portable code-first simulator kernel: `Simulator.define`, the
 * closed EventCatalog, the runtime roster, scoped lifecycle, and the
 * root-exported `StackProvider` contract.
 *
 * The simulator is a system driver around the single production stack's
 * public capabilities. It does not create an alternative Router or Ledger
 * and does not sit inside production traffic.
 */

export {};
