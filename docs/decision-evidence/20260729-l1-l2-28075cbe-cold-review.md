# L1 and L2 authority blind review — `28075cbe`

## Record metadata

| Field | Value |
|---|---|
| Review run ID | `l1-l2-28075cbe` |
| Candidate commit | `28075cbe8eb4abd5eab0bc4dbf0998de7b35131a` |
| Candidate tree | `80ad7bb24452391835786881b676e84ba798d363` |
| Candidate content digest | SHA-256 `7ae8f7f3df5e1b3f8b0ee9e2ac47d585d72595ab8a6d56604c0272f8fdee5531` |
| Digest scope and command | Whole candidate tree, `git archive --format=tar 28075cbe8eb4abd5eab0bc4dbf0998de7b35131a \| sha256sum` |
| Reviewer | fresh Codex teammate session `/root/blind_candidate_review` |
| Review started | `2026-07-29T20:57:00Z` |
| Review finished | `2026-07-29T21:02:01Z` |
| Review duration | `00:05:01` |
| Review budget | one uninterrupted fresh-agent context, maximum 45 minutes |
| Rerun of | none |
| Rerun reason | none |

## Reviewer submission — verbatim

# Blind teammate review record

## Candidate identity

- Candidate commit: `28075cbe8eb4abd5eab0bc4dbf0998de7b35131a`
- Parent: `5d0cca97ae68479dbe7e0aa329fb7921b68b3b83`
- Subject: `docs(v2): freeze L1 and L2 implementation contract`
- Reviewed repository root: `/home/tapanc/moltzap`
- HEAD matched the candidate throughout the review.
- Review started: `2026-07-29T20:57:00Z`
- Review ended: `2026-07-29T21:02:01Z`
- Wall-clock duration: `00:05:01`
- Repository modifications: none; final `git status --short` was empty.
- `git diff --check` passed.

## Reviewer identity and isolation attestation

Reviewer: fresh Codex teammate session `/root/blind_candidate_review`.

I did not author or reconcile this candidate. I received only the candidate identity, repository root, fixed questions, and isolation/quarantine rules. I received no design summary, diff tour, ADR or file pointer, search term, expected answer, earlier review result, inherited author conversation, compaction, memory, private state, or author coaching.

I did not open, read, or search the contents of any `*-cold-review.md` or invalid-review record. All repository searches explicitly excluded those patterns. No command returned an answer or verdict from a quarantined record. Merely seeing the generic quarantine rules in `AGENTS.md` did not expose a quarantined artifact.

## Exact prompt

1. What decision does this candidate make current, what problem does it
   resolve, and which statements are binding versus context or
   non-normative explanation?
2. What earlier outcomes does it replace, retain, or leave untouched,
   and where does the current normative contract live?
3. What must an implementer now do or avoid, which layers or consumers
   are affected, and under what fault, trust, safety, liveness, and
   compatibility assumptions?
4. Which humans are named as decision-makers, which source events does
   the compacted trajectory cite for their calls, alternatives,
   reversals, and deferrals, and what source gaps does it explicitly
   record? Report only what the event ledger states; do not infer
   motives, confidence, urgency, or rationale.
5. Find the strongest apparent contradiction, stale instruction, or
   broken lineage elsewhere in the repository. Resolve it using the
   authority order or report it as a blocker.
6. Could a teammate implement the decision without chat or guessing?
   List every missing link or unresolved choice and classify each as a
   deliberate deferral or an accidental gap.

## Unedited answers

### 1. What decision does this candidate make current, what problem does it
   resolve, and which statements are binding versus context or
   non-normative explanation?

The candidate makes four related decisions current:

1. V2 authority lives on the `v2` track. `main` remains authoritative for V1 and continues merging forward, but V2 ADRs/specifications need no main-branch duplicate before becoming current.
2. L1 and L2 own separate representation chapters and acceptance evidence. There is no current cross-layer wire catalog, generic codec/wire package, or shared vector-corpus abstraction.
3. L1 uses exact JCS JSON, Ed25519 public JWKs, attached General JWS AgentCards and SignedMessages, and an identity-owned deep `AuthenticatedHttp` RFC 9421 boundary. Registry lookup/list are public reads. Application TLS and channel protection belong to deployment.
4. Router order is private. Public continuation is an opaque client-held Compact-JWE PollCursor; accepted sends return RouterInstanceId and SignedMessageDigest; Router retains one bounded volatile global feed and coupled retry state. The package is `router`, not `transport`.

These resolve four conflicts: main-first V2 governance; a cross-layer X.509/CBOR/COSE byte catalog; identity/authentication complexity and custom-library pressure; and a Router contract that exposed internal sequence positions or invited durable/per-recipient delivery state.

Binding authority is:

- `AGENTS.md` and `v2/VISION.md`;
- current ADR Decision Outcomes, including the explicitly retained scope of partially superseded records;
- normative `docs/spec/` chapters;
- the freeze manifest’s current trace rows and explicit deferrals.

For this candidate, the exact L1/L2 interface and bytes live in `docs/spec/identity.md`, `identity-representation.md`, `router.md`, `router-representation.md`, and the package/type laws in `layer-interfaces.md`.

ADR context, considered options, historical bodies, and consequences explain the decision but do not override the current outcome. The compacted trajectory is explicitly non-normative evidence. `docs/architecture/l1-l2-implementation-ask.md` is an execution handoff, not protocol authority. Its implementation guidance must yield to the governing ADR/spec contract.

### 2. What earlier outcomes does it replace, retain, or leave untouched,
   and where does the current normative contract live?

Fully replaced outcomes include:

- the main-first V2 specification rule;
- the cross-layer Gate 1 wire profile and corpus;
- X.509 as AgentCard container;
- the interim request-signature profile;
- registration as an out-of-band library operation;
- X.509/CBOR/COSE L1 representation and mandatory application-facing TLS.

Partially replaced records retain these scopes:

- MoltZap-native, principal-shaped, immutable complete AgentCards;
- one long-lived Ed25519 agent credential, with registration as the pre-card exception;
- Registry, Router, Ledger, and daemon process separation;
- independently authenticated, sessionless HTTP operations and bounded Router long polling;
- self-contained SignedMessage attribution independent of HTTP carriage;
- exactly six deep packages, one CalVer, the package DAG, and zero V1 imports;
- the code-first simulator kernel and system-driver boundary, but not simulator-owned Router state or public Router positions;
- the repository-native freeze, explicit lineage, traceability inventory, blind-review gate, layer constitution, fault assumptions, later-layer semantics, and deferrals.

The package count remains six, while `transport` becomes `router` and `moltzap-directory` becomes `moltzap-registry`.

The candidate explicitly leaves L3 Transcript/Ledger semantics, OpenFloorV1, L4, endpoint-daemon, local MCP, model-surface, and later trust-layer semantic outcomes unchanged. It assigns no replacement representation to L3 or later and does not permit the superseded cross-layer wire profile to fill that gap.

Current normative ownership is discoverable from:

- `AGENTS.md` and `v2/VISION.md`;
- the four accepted 2026-07-29 ADRs;
- `docs/decisions/20260728-gate-1-architecture-freeze.md` → updated traceability inventory;
- `docs/spec/README.md` → L1 and L2 representation readiness;
- `docs/spec/identity.md` and `identity-representation.md`;
- `docs/spec/router.md` and `router-representation.md`;
- `docs/spec/layer-interfaces.md`.

Historical records visibly state retained and replaced portions and point to those owners.

### 3. What must an implementer now do or avoid, which layers or consumers
   are affected, and under what fault, trust, safety, liveness, and
   compatibility assumptions?

An implementer must:

- Complete and obtain acceptance of this authority/blind-review gate before L1/L2 product code starts.
- Implement `identity` as the deep L1 package: branded values, strict canonical base64url, JCS decoding, exact Ed25519 JWK validation, exact General JWS AgentCard/SignedMessage profiles, AuthenticatedHttp, Registry client/server, PostgreSQL persistence, registration idempotency, public lookup/list, replay protection, and `moltzap-registry`.
- Implement `router` as the deep L2 package: Router client/server, private bigint order, one count-and-byte-bounded global feed, coupled retry index, caller-card cache, nonce state, opaque Compact-JWE cursor, send/poll/health routes, restart fencing, long polling, and `moltzap-router`.
- Use exact routes, closed results/errors, validation order, fixed JSON depth 16, fixed 25-second poll hold, and `2026.729.1` representations.
- Keep maintained JCS, JOSE, RFC 9421, structured-field, Effect, SQL, and PostgreSQL mechanisms behind narrow private adapters.
- Preserve package and process separation and run work through the prescribed slices and human readability/vocabulary gates.

An implementer must avoid:

- public RouterSequence or any public order position;
- durable Router feed/cursors, recipient queues, recipient copies, progress, sessions, conversations, transactions, recovery, or acknowledgments;
- interpreting the SignedMessage body in L1/L2;
- embedding AgentCard in normal requests;
- X.509, deterministic CBOR, COSE, MessagePack, custom crypto/canonicalization frameworks, or a shared cross-layer adapter/package for L1/L2;
- application TLS, scheme, certificate, or trusted-proxy policy;
- a seventh package, `transport`, `wire`, `protocol`, or other shallow mechanism package;
- imports from `packages/*` or forbidden V2 dependency edges;
- changing L3+ representations or silently normalizing `HarnessEndpoin`.

Affected surfaces include all six package manifests and dependency references, the Registry and Router binaries, endpoint clients and daemon integration, transcript’s permitted dependency on Router contracts, simulator/testbed composition, CLI bootstrap/read behavior, architecture checks, constants, and external consumers that must remain on public interfaces. L3+ semantics remain unchanged.

Fault/trust assumptions:

- Endpoints may be Byzantine.
- Gate 1 assumes one correct non-equivocating Registry, one correct non-equivocating Router, and one correct durable Ledger.
- A malicious/equivocating Registry and Byzantine Router sequencing are outside the guarantee.
- Registry outage blocks registration and uncached identity resolution, but pinned cards and self-contained records remain verifiable.
- Router or Ledger unavailability may stop progress.
- Router restart discards feed, retry, nonce, card-cache, cursor-key, and waiter state, produces a new instance, and provides fail-stop fencing rather than transparent liveness.
- Safety does not depend on timing. Upper-layer progress additionally requires all fixed members and required services to act within the 90-second protocol TTL.
- Signed HTTP requests provide authentication/integrity, not confidentiality; deployment protects admission credentials and other traffic where required.

Compatibility assumptions:

- V2 has no V1 compatibility obligation and imports no V1 packages.
- All six V2 manifests and ready MoltZap representations equal `2026.729.1`.
- MCP revision `2026-07-28` and simulator persisted-schema versions remain independent.
- V1 continues shipping/publishing from `main` until a future cutover decision.

### 4. Which humans are named as decision-makers, which source events does
   the compacted trajectory cite for their calls, alternatives,
   reversals, and deferrals, and what source gaps does it explicitly
   record? Report only what the event ledger states; do not infer
   motives, confidence, urgency, or rationale.

All four new ADRs name Tapan Chugh as decision-maker.

The trajectory cites one Codex CLI rollout session,
`019fac90-d26a-7e51-8708-06858bd118bd`, and records these material events:

- V2 authority:
  - `call_Ng5f0fLPyIrxjfHwz7biWabP` presented v2-owned authority versus main-first.
  - `fco_019facb5-464c-7ec0-9230-1d147fa2b9ef` selected `Own them on v2`.

- Layer-owned representations:
  - `msg_019facba-9099-7372-8a85-ee9af4171e51` gated vocabulary changes on humans and questioned wire-catalog/test-vector terminology.
  - `msg_019fae74-8382-7530-b3b9-1d6dd3ed5e3e` said `separate documents one per layer`.
  - `call_LA8otttI0rHNqxo8xHkGWTGV` presented shared conventions, repetition per layer, or L1 ownership.
  - `fco_019fae76-9fff-7043-95be-d074a0b69464` selected `Repeat per layer`.
  - `msg_019fae76-f1c4-7612-902e-9763718af9ab` asked why cross-layer things were needed.
  - The following withdrawal was an assistant reconciliation, not a human event.

- Identity/JCS/JOSE/AuthHttp:
  - `msg_019fae7c-8c8d-7802-8c93-a50a36905623` raised MessagePack. The structured MessagePack choice was literally aborted and recorded no selection.
  - `msg_019fae7e-e68e-7502-948e-527f704632ba` asked to minimize custom libraries/technical debt.
  - `msg_019fae86-54db-7be0-9bb0-42fe342ccd39` raised JOSE.
  - `fco_019fae87-669d-7071-a217-67d52b651646` selected `Adopt JOSE/JSON`.
  - `fco_019fae8c-c55e-7a11-8e3d-e8b6844146a5` selected JSON JWS forms and opaque AgentId but selected no JCS-scope option.
  - `fco_019fae90-a2ae-7ab0-a7c8-6989db26f8d2` asked whether option 3 reduced maintenance but did not itself select it.
  - `fco_019fae92-c987-7c02-896b-f57d45c730b1` selected canonical network JSON.
  - `fco_019fae97-3f50-73b1-8e30-1b25eeb257db` selected General JWS everywhere, a shared adapter library, and safe JSON numbers. The public Router-position premise was later superseded.
  - `msg_019fae9b-223b-7792-a78d-016a181937d2` invoked Ousterhout/deep modules.
  - `fco_019fae9c-b6c7-7860-9dc6-99cc761d20d2` selected no listed replacement-adapter option and said the capability might be subsumed into identity/transport.
  - `fco_019faea3-ef76-7623-a5a4-08c4221d4071` selected ownership inside identity.
  - `fco_019faec0-0da3-7142-a3d0-fcbc6fa4e3d8` selected `AuthenticatedHttp`, Registry lookup rather than carried AgentCard, and one collapsed authentication failure.
  - `fco_019faf1f-7822-7bc0-aaad-3b69af0d0e25` selected a deep narrow AuthenticatedHttp boundary.

- Router:
  - `msg_019fae9d-a944-7740-b320-84b6da3225d4` distinguished control/data paths and questioned the `transport` package.
  - `fco_019faea1-3183-74f0-8ff4-250e7bf001fe` selected rename to `router`.
  - `fco_019faea7-98b8-72b0-a807-fa2c5aaacaf8` questioned whether sequence numbers needed to be public and selected JWK thumbprint URI plus media types.
  - `fco_019faea8-efeb-7523-bf52-a3902b63ddf1` selected opaque positions.
  - `fco_019faec4-022b-7291-a4a1-2a79c4ecefaf` selected domain-ID pagination, opaque PollCursor, and accepted-message digest.
  - `msg_019faec4-0251-7d00-ac49-3df4b0c7e1d1` said `record this alternative`; the referent among three preceding questions is explicitly recorded as ambiguous.
  - `fco_019faec7-667a-78f0-94b6-53c6f5ceed82` initially deferred poll-restart fencing.
  - `fco_019faeeb-78aa-73c0-9aea-8691300059d6` later selected old cursor as invalid rather than an expected-instance poll field.
  - `msg_019faefb-d4dd-7d72-b85d-177f665e9707` required a lightweight design without server-side cursor state.
  - `fco_019faefc-cd94-7bb3-a64d-7c0a34fb0c02` selected Compact JWE.
  - `fco_019faefe-d122-7e72-ada6-75469050d7ac` initially selected deployment-edge TLS, including client plain-HTTP rejection.
  - `msg_019faeff-730d-73a3-9343-72b3d40a69d6` subsequently said TLS is fully a deployment concern and should not be mandatory in code.
  - `fco_019faf09-39ca-7381-aba7-0659ab2d4333` selected one global ring.

- Scope and deferral:
  - `msg_019faf74-e678-7ab3-8b27-090394ccf237` limited implementation to L1/L2.
  - `msg_019faf77-25d7-7f41-aa0d-4784aac3162f` directed restoration of changed L3/L4 documents.
  - `msg_019faf78-2bdf-7113-a819-75d66749f12d` rejected the wire profile.
  - `msg_019faf79-b813-7583-997f-21a260009256` retained the discussed later-layer vocabulary.
  - `msg_019faf87-4005-7b21-8899-fa24637414a1` directed the revised plan to become the end-to-end goal.
  - `msg_019faf30-1225-75c1-b6c0-bf1fe7dae82a` requested durable storage of the implementation ask.
  - `msg_019faf44-02bc-7b91-a782-cbf1c659ab24` omitted the ACG upgrade for now.

Explicit source gaps:

- Session metadata does not bind the session account to Tapan Chugh, and structured input outputs lack a stored human actor role. Naming him is an accountability statement requiring his review, not something proven by session metadata.
- `HarnessEndpoin` was literally selected; `HarnessEndpoint` was an agent normalization with no later literal human confirmation.
- `record this alternative` has an ambiguous referent.
- The MessagePack prompt was aborted and records no selection.
- Shared-adapter ownership was revisited through several events; the ledger does not infer a motive.
- The later TLS instruction broadened/reversed the narrower initial selection.
- Stateless PollCursor does not mean a completely stateless Router.
- No original public human event was found for the code-first simulator-kernel decision; this is recorded as a source gap rather than reconstructed rationale.

### 5. Find the strongest apparent contradiction, stale instruction, or
   broken lineage elsewhere in the repository. Resolve it using the
   authority order or report it as a blocker.

Blocker found in `docs/spec/control-plane.md`, a current document marked `Gate 1 normative`.

Under `Purpose and boundary`, it characterizes the control plane as individually authenticated HTTP operations. Under `Common HTTP contract`, it says every domain POST:

- carries the exact `moltzap-protocol` value from `v2/VERSION`; and
- is authenticated independently.

That includes the Registry’s lookup and list POST routes.

The current accepted L1 decision and exact L1 owner instead say:

- Registry lookup and list are public unauthenticated reads;
- L1/Router requests use the exact `MoltZap-Version` field;
- current exact framing is owned by `docs/spec/identity-representation.md`.

The freeze manifest agrees: G1-DEC-208 makes lookup/list public unauthenticated reads, and G1-DEC-214/223 assign the current AuthenticatedHttp/version contract.

Authority order identifies the intended behavior: the new accepted ADR and the specific exact L1 representation owner govern over the stale generic language. But that does not make the candidate contradiction-free. `docs/spec/README.md` says a conflict among current authority/specifications is a documentation defect and implementation stops; freeze row G1-DEC-008 says the authority gate fails on any contradiction.

Therefore this is an unresolved normative contradiction and a blind-gate blocker, not merely historical text that can be ignored.

A separate historical-looking main-first instruction remains in the partially superseded freeze body, but its visible Supersession section and the newer accepted V2-authority ADR resolve it cleanly. It is not the blocker above.

### 6. Could a teammate implement the decision without chat or guessing?
   List every missing link or unresolved choice and classify each as a
   deliberate deferral or an accidental gap.

No, not from this exact candidate while obeying the repository’s stop-on-contradiction rule.

The intended L1/L2 contract is otherwise highly discoverable and detailed: exact values, bytes, headers, validation order, routes, results, state model, failure semantics, package graph, version, tests, and implementation slices are all present. However, the current normative `control-plane.md` authentication/version statements conflict with the accepted L1 outcome and exact L1 owner. That is an accidental gap and blocks implementation until reconciled.

Accidental gap:

- Current normative `docs/spec/control-plane.md` still requires authentication for every domain POST and names `moltzap-protocol`, contradicting public unauthenticated Registry reads and the exact `MoltZap-Version` L1 profile.

Deliberate staged human gates, not implementation discretion:

- Configuration-prefix suffixes require literal maintainer approval before entering source.
- Any new public export or vocabulary not already approved stops for human review.
- Every implementation slice requires a human readability disposition.

Deliberate deferrals/out-of-scope choices:

- No L3-or-later replacement representation is assigned by this candidate.
- The future `HarnessEndpoin` spelling is not normalized or implemented.
- Key rotation, revocation, recovery, delegation, encrypted keys, keychains, HSMs, external signers, and malicious-Registry tolerance.
- Router persistence, replication, failover, Byzantine sequencing, fork detection, stable instance identity, per-recipient state/indexes, offline convergence, transparent restart, and push transport.
- Required end-to-end body encryption/key distribution and application-owned TLS.
- Conversations, reliability, transactions, replay, and recovery at L2.
- Publishing from V2, deployment, cutover, V1 retirement, and branch consolidation.
- Simulator porting until its separate immutable-source handoff is complete.
- The broader unchanged Gate 1 deferrals enumerated as G1-DEC-800 through G1-DEC-824.

These deliberate deferrals have explicit stop boundaries and do not require guessing. The accidental normative contradiction does.

## Independently discovered paths and headings

- `AGENTS.md`
  - Project
  - Constitution
  - Architecture decision records
  - Decision provenance
  - Lifecycle and landing
  - Blind teammate review gate
- `v2/AGENTS.md`
  - Authority and reading order
  - Structure
  - Implementation rules
  - Simulator provenance gate
- `v2/VISION.md`
  - Authority
  - The constitution
  - Gate 1 profile
  - Trust and failure envelope
  - Processes and persistence
  - Packages and versions
  - Open-question register
- `docs/decisions/README.md`
  - Canonical reading guidance
  - Records
- `docs/decisions/20260729-v2-authority-lives-with-v2.md`
  - Decision Outcome
- `docs/decisions/20260729-representations-are-layer-owned.md`
  - Decision Outcome
  - Scope boundary
- `docs/decisions/20260729-identity-uses-jcs-jose-authenticated-http.md`
  - Decision Outcome
- `docs/decisions/20260729-router-order-is-opaque.md`
  - Decision Outcome
- `docs/decisions/20260728-gate-1-architecture-freeze.md`
  - Supersession
  - Gate 1 traceability inventory
  - Explicit deferrals
  - Gate 0 review record
- `docs/decision-evidence/20260729-l1-l2-implementation-trajectory.md`
  - V2 authority lives with V2
  - Representations are layer-owned
  - Identity uses JCS, JOSE, and AuthenticatedHttp
  - Router order is opaque
  - L1 and L2 only scope
  - Cross-cutting source gaps
  - Simulator provenance source gap
- `docs/spec/README.md`
  - Authority and reading order
  - L1 and L2 representation readiness
- `docs/spec/identity.md`
  - Trust and failure assumptions
  - AgentCard
  - Registration
  - AuthenticatedHttp
  - Explicitly deferred
- `docs/spec/identity-representation.md`
  - Canonical JSON
  - AgentCard
  - SignedMessage
  - AuthenticatedHttp validation order
  - Registry routes
- `docs/spec/router.md`
  - Process and trust model
  - Send
  - One volatile global feed
  - Poll
  - Feed gap and restart recovery
  - Explicitly deferred
- `docs/spec/router-representation.md`
  - PollCursor
  - Send
  - Poll
- `docs/spec/layer-interfaces.md`
  - Package graph
  - L1 and L2 representation ownership
  - Cross-layer laws
- `docs/spec/control-plane.md`
  - Purpose and boundary
  - Common HTTP contract
- `docs/architecture/l1-l2-implementation-ask.md`
  - Human gates
  - Authority gate
  - Implementation slices
  - Explicit deferrals
- `docs/architecture/first-implementation.md`
  - Gate 0
  - Gate 2
  - Gate 3
  - Gate 4
- The visible `Supersession` sections of every modified historical ADR.
- `v2/VERSION` and all six `v2/*/package.json` manifests.

## Concise discovery trail

1. Confirmed repository root, HEAD, parent, candidate subject, clean worktree, and applicable root/V2 agent law.
2. Listed candidate paths and independently found the decision index.
3. Read the authority chain beginning with `v2/VISION.md` and current ADR index.
4. Read the four new ADRs, freeze manifest, relevant partially/fully superseded ADR headers and Supersession sections.
5. Read the source-event trajectory and its explicit source-gap sections.
6. Followed trace rows into the L1/L2 semantic and representation owners, package laws, and architecture handoff.
7. Checked manifests/version/DAG and searched current authority/spec/architecture text for stale package, wire, TLS, sequence, authentication, and version vocabulary while excluding quarantined artifacts.
8. Compared the current L1 exact contract against the still-current control-plane common contract and found the blocker.
9. Ran `git diff --check` and verified the worktree remained clean.

## Author interventions

None. No author message, hint, correction, or coaching was received during the run.

## Per-question verdicts

| Question | Verdict | Blocker |
|---|---|---|
| 1 | PASS | Current decisions, problems, and binding/non-binding authority were discoverable. |
| 2 | PASS | Replacement, retained scope, untouched later layers, and normative owners were discoverable with consistent ADR lineage. |
| 3 | PASS | Implementation duties, prohibited designs, affected consumers, fault/trust assumptions, safety/liveness boundaries, and compatibility rules were discoverable. |
| 4 | PASS | Decision-maker attribution, source events, alternatives, reversals, deferrals, and explicit source gaps were discoverable without inferring motive or rationale. |
| 5 | FAIL | Current normative `control-plane.md` contradicts the accepted/exact L1 authentication and version contract. |
| 6 | FAIL | A teammate must stop on that accidental normative contradiction; the remainder is implementable only after reconciliation. |

## Overall verdict

**FAIL**

The exact candidate does not satisfy the contradiction-free blind teammate gate. The blocking issue is the stale current-normative authentication/version language in `docs/spec/control-plane.md`.

## Maintainer acceptance

| Field | Value |
|---|---|
| Maintainer | pending |
| Reviewed result | `l1-l2-28075cbe` |
| Candidate identity matches | pending |
| Gate decision | pending |
| Decision time | pending |
| Rationale | Failed review is preserved; a corrected candidate requires a different fresh reviewer. |

## Rerun identity

| Field | Value |
|---|---|
| Superseded by review run | pending |
| Superseded candidate commit | pending |
| Superseded candidate content digest | pending |
| Reason a rerun was required | Current normative `docs/spec/control-plane.md` contradicted public Registry reads and the exact L1 version header. |
