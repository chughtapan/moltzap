# Router-ordered action signatures source gap

The active Codex conversation considered relevant by the authoring agent did
not expose a native source-session identifier, message or event locators,
enclosing turn and parent locators, or exact UTC message timestamps. No
source-faithful event row can therefore be retained without inventing required
metadata, and this report does not state what decision the missing events made.

## Searches checked

- The active conversation context supplied to the authoring agent contained no
  native source metadata beyond stored actor roles.
- The repository decision-evidence catalog documents the required native
  locators and exact timestamps but contains no source-gap artifact for this
  exchange.
- The current addressed-messaging trajectory establishes the retained
  first-Router-ordered-candidate law, but it predates this representation
  decision and does not establish the new call.

No source event located. The missing session identifier, native locators, and
exact timestamps are an explicit source gap. This report does not reconstruct
the exchange from ADR prose, repository changes, or memory. If the original
session export becomes available, preserve it as a dated correction rather
than silently rewriting this report.
