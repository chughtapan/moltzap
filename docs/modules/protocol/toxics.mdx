---
title: "protocol/toxics"
description: "Public barrel for Toxiproxy toxic profiles and control helpers."
---

# protocol/toxics

_`packages/protocol/src/testing/toxics`_

## Purpose

Public barrel for Toxiproxy toxic profiles and control helpers.

## Public surface

### [`allToxicTags`](./index.ts#L7)

_Reference_

### [`allToxicTags`](./profile.ts#L51)

_Variable_

All six toxic tags, enumerated for coverage assertions in Tier D.

### [`apiUrl`](./client.ts#L27)

_Property_

Control-plane URL, e.g. `http://localhost:8474`.

### [`body`](./errors.ts#L13)

_Property_

### [`defaultToxicProfile`](./index.ts#L11)

_Reference_

### [`defaultToxicProfile`](./defaults.ts#L24)

_Variable_

### [`deliveryInvariantFor`](./index.ts#L9)

_Reference_

### [`deliveryInvariantFor`](./profile.ts#L75)

_Function_

### [`DeliveryInvariantName`](./index.ts#L8)

_Reference_

### [`DeliveryInvariantName`](./profile.ts#L68)

_TypeAlias_

Every delivery-layer property one toxic is expected to re-exercise.
Keyed by the property's semantic name (matches the register-function
names in `conformance/task/{fan-out-cardinality,store-and-replay,
payload-opacity,task-boundary-isolation}.ts`).

### [`listenUrl`](./client.ts#L50)

_Property_

Client-facing URL (`ws://127.0.0.1:&lt;ephemeralPort>`).

### [`makeToxiproxyClient`](./index.ts#L17)

_Reference_

### [`makeToxiproxyClient`](./client.ts#L231)

_Function_

### [`name`](./client.ts#L37)

_Property_

### [`op`](./errors.ts#L11)

_Property_

### [`ping`](./client.ts#L64)

_Property_

Probe: control plane reachable.

### [`profile`](./client.ts#L38)

_Property_

### [`proxy`](./client.ts#L59)

_Property_

Create a scoped proxy; teardown on release.

### [`status`](./errors.ts#L12)

_Property_

### [`testing/toxics`](./index.ts#L1)

_Module_

### [`testing/toxics/client`](./client.ts#L1)

_Module_

### [`testing/toxics/defaults`](./defaults.ts#L1)

_Module_

### [`testing/toxics/errors`](./errors.ts#L1)

_Module_

### [`testing/toxics/profile`](./profile.ts#L1)

_Module_

### [`ToxicControlError`](./index.ts#L19)

_Reference_

### [`ToxicControlError`](./errors.ts#L8)

_Class_

Toxiproxy HTTP API returned a non-2xx, or the control endpoint is down.

### [`ToxicHandle`](./index.ts#L16)

_Reference_

### [`ToxicHandle`](./client.ts#L36)

_Interface_

A live toxic attachment. Scoped: acquiring adds the toxic to the proxy,
releasing the scope removes it. Tier D properties acquire a
`ToxicHandle` inside `Effect.scoped` so a crashed property still cleans
up.

### [`ToxicProfile`](./index.ts#L5)

_Reference_

### [`ToxicProfile`](./profile.ts#L14)

_TypeAlias_

Toxic profile DSL.

Per D2 and Invariant I4, adversity is a parameter selected at suite
invocation, not hardcoded case-by-case. A `ToxicProfile` is a named
preset (one of the six toxics) plus its parameters; the Tier D runner
picks the matching Tier C invariant and re-runs it with the toxic
attached.

Exhaustiveness: the `_tag` union covers every toxic named in §5 Tier D
(D1–D6) so the implementer cannot forget a branch in the client dispatch.

### [`ToxicTag`](./index.ts#L6)

_Reference_

### [`ToxicTag`](./profile.ts#L60)

_TypeAlias_

### [`ToxiproxyClient`](./index.ts#L13)

_Reference_

### [`ToxiproxyClient`](./client.ts#L57)

_Interface_

### [`ToxiproxyConfig`](./index.ts#L14)

_Reference_

### [`ToxiproxyConfig`](./client.ts#L25)

_Interface_

### [`ToxiproxyProxy`](./index.ts#L15)

_Reference_

### [`ToxiproxyProxy`](./client.ts#L46)

_Interface_

A live proxy that sits between TestClient and the real server (or
between real client and TestServer). Acquiring the scope allocates an
ephemeral port and registers the proxy; releasing deletes it.

### [`upstream`](./client.ts#L48)

_Property_

Upstream (real server) address the proxy forwards to.

### [`withToxic`](./client.ts#L52)

_Property_

Attach a toxic inside a Scope; removed on release.

## Files

- `client.ts`
- `defaults.ts`
- `errors.ts`
- `index.ts`
- `profile.ts`
