---
title: "task/conversation/create"
description: "TM only — create a new conversation under an existing task. Participants must already be in task_participants."
---

# task/conversation/create

TM only — create a new conversation under an existing task. Participants must already be in task_participants.

## Parameters

<ParamField path="taskId" type="string (UUID)" required>
  Branded TaskId
</ParamField>

<ParamField path="name" type="string">
  The name field.
</ParamField>

<ParamField path="participants" type="array" required>
  The participants field.
</ParamField>

## Response

<ResponseField name="conversation" type="object">
  The conversation field.
</ResponseField>

## Errors

| Code | Name | When |
|------|------|------|
| -32023 | ParticipantNotAdmitted | Agent is not admitted to the task |

## Related Notifications

- [`task/conversation/created`](/protocol/notifications/task-conversation-created)
