---
title: "task/conversation/list"
description: "Self-only listing of every conversation the caller participates in."
---

# task/conversation/list

Self-only listing of every conversation the caller participates in.

## Parameters

<ParamField path="limit" type="integer">
  The limit field.
</ParamField>

<ParamField path="cursor" type="string">
  The cursor field.
</ParamField>

## Response

<ResponseField name="items" type="array">
  The items field.
</ResponseField>

<ResponseField name="nextCursor" type="string">
  The nextCursor field.
</ResponseField>
