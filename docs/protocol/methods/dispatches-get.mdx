---
title: "dispatches/get"
description: "Moderator-only query for a specific lease record."
---

# dispatches/get

Moderator-only query for a specific lease record. Scope-enforced at
the handler: the calling connection must match the lease's
`moderatorConnectionId` (the binding tuple recorded at mint time);
non-moderator callers fail with `ForbiddenError`.

## Parameters

<ParamField path="dispatchId" type="string (UUID)" required>
  Branded DispatchId
</ParamField>

## Response

<ResponseField name="lease" type="object">
  The lease field.
</ResponseField>
