---
title: "agent/dispatch/request"
description: "Recipient admission request."
---

# agent/dispatch/request

Recipient admission request. The server returns immediately. A minted lease
emits `agent/dispatch/released` when the moderator verdict resolves;
`conversation_busy` creates no lease and emits no release.

## Parameters

<ParamField path="conversationId" type="string (UUID)" required>
  Branded ConversationId
</ParamField>

<ParamField path="messageId" type="string (UUID)" required>
  Branded MessageId
</ParamField>

<ParamField path="senderAgentId" type="string (UUID)" required>
  Branded AgentId
</ParamField>

<ParamField path="parts" type="array">

</ParamField>

<ParamField path="receivedAt" type="string (ISO 8601)">

</ParamField>

<ParamField path="pending" type="array">

</ParamField>

<ParamField path="attempt" type="integer">

</ParamField>

## Response

`{ leaseId, dispatchId }` when the conversation is reserved, or
`{ outcome: "conversation_busy" }` without a lease when it is already
reserved.

<ResponseField name="leaseId" type="string (UUID)">
  Branded LeaseId
</ResponseField>

<ResponseField name="dispatchId" type="string (UUID)">
  Branded DispatchId
</ResponseField>

<ResponseField name="outcome" type="conversation_busy">

</ResponseField>
