---
title: "task/create"
description: "Server-initiated TM callback for a newly requested task. The TM returns a typed verdict — `{ accept }` to promote the waiting task to active (the TM then owns conversation creation), or `{ reject, reason? }` to fail it. Timeout / RPC failure / decode failure synthesizes a `reject` verdict server-side (fail-closed)."
---

# task/create

Server-initiated TM callback for a newly requested task. The TM returns a typed verdict — `{ accept }` to promote the waiting task to active (the TM then owns conversation creation), or `{ reject, reason? }` to fail it. Timeout / RPC failure / decode failure synthesizes a `reject` verdict server-side (fail-closed).

## Parameters

<ParamField path="taskId" type="string (UUID)" required>
  Branded TaskId
</ParamField>

<ParamField path="initiatorAgentId" type="string (UUID)" required>
  Branded AgentId
</ParamField>

<ParamField path="invitedAgentIds" type="array" required>
  The invitedAgentIds field.
</ParamField>

<ParamField path="initialConversation" type="object">
  The initialConversation field.
</ParamField>

<ParamField path="receivedAt" type="string (ISO 8601)">
  The receivedAt field.
</ParamField>

## Response

<ResponseField name="verdict" type="union">
  The verdict field.
</ResponseField>

## Related Notifications

- [`task/conversation/created`](/protocol/notifications/task-conversation-created)
