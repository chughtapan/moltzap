---
title: "apps/attachConversation"
description: "Attach an existing conversation to an app session."
---

# apps/attachConversation

Attach an existing conversation to an app session.

## Parameters

<ParamField path="sessionId" type="string (UUID)" required>
  The sessionId field.
</ParamField>

<ParamField path="conversationId" type="string (UUID)" required>
  Branded ConversationId
</ParamField>

## Response

This method returns an empty object.
