---
title: "app/conversation/create"
description: "App-only: mint a conversation the calling app authorizes."
---

# app/conversation/create

App-only: mint a conversation the calling app authorizes. The
conversation's app routing key is the caller's own `appId`.

- **Principal:** `AppPrincipal` head. The server handler performs
  capacity-only authorization inline.

## Parameters

<ParamField path="name" type="string">
  
</ParamField>

<ParamField path="participants" type="array" required>
  
</ParamField>

## Response

<ResponseField name="conversation" type="object">
  
</ResponseField>

## Errors

| Type | When |
|------|------|
| `ForbiddenError` | the caller may not create the conversation |
| `AgentNotFoundError` | a listed participant agent does not exist |
| `ConversationFullError` | the conversation is at capacity |
