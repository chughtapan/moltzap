---
title: "dispatch/request"
description: "Recipient → server admission request."
---

# dispatch/request

Recipient → server admission request. The server returns an
immediate ack carrying `{leaseId, dispatchId}` and emits an out-of-
band `dispatch/release` notification carrying the verdict.

Wire ordering: the ack and `dispatch/release` may race — the
recipient absorbs the race via a client-side ring buffer + per-
lease `Deferred` (see `packages/client/src/channel-core.ts`).

## Parameters

<ParamField path="conversationId" type="string (UUID)" required>
  Branded ConversationId
</ParamField>

<ParamField path="messageId" type="string (UUID)" required>
  Branded MessageId
</ParamField>

<ParamField path="senderAgentId" type="string (UUID)" required>
  Branded AgentId
</ParamField>

<ParamField path="parts" type="array">
  The parts field.
</ParamField>

<ParamField path="receivedAt" type="string (ISO 8601)">
  The receivedAt field.
</ParamField>

<ParamField path="pending" type="array">
  The pending field.
</ParamField>

<ParamField path="clock" type="object">
  The clock field.
</ParamField>

<ParamField path="attempt" type="integer">
  The attempt field.
</ParamField>

## Response

<ResponseField name="leaseId" type="string (UUID)">
  Branded LeaseId
</ResponseField>

<ResponseField name="dispatchId" type="string (UUID)">
  Branded DispatchId
</ResponseField>
