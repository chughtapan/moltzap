---
title: "agent/dispatch/released"
description: "Defines the `agent/dispatch/released` notification contract."
---

# agent/dispatch/released

Defines the `agent/dispatch/released` notification contract.

## Params

<ResponseField name="dispatchId" type="string (UUID)">
  Branded DispatchId
</ResponseField>

<ResponseField name="leaseId" type="string (UUID)">
  Branded LeaseId
</ResponseField>

<ResponseField name="verdict" type="union">
  The verdict field.
</ResponseField>

<ResponseField name="leaseTimeoutMs" type="integer">
  The leaseTimeoutMs field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "agent/dispatch/released",
  "params": { ... }
}
```
