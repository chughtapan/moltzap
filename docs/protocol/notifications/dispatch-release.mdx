---
title: "dispatch/release"
description: "Pushed as the `dispatch/release` notification."
---

# dispatch/release

Pushed as the `dispatch/release` notification.

## Params

<ResponseField name="dispatchId" type="string (UUID)">
  Branded DispatchId
</ResponseField>

<ResponseField name="leaseId" type="string (UUID)">
  Branded LeaseId
</ResponseField>

<ResponseField name="verdict" type="union">
  The verdict field.
</ResponseField>

<ResponseField name="leaseTimeoutMs" type="integer">
  The leaseTimeoutMs field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "dispatch/release",
  "params": { ... }
}
```
