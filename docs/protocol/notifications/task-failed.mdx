---
title: "task/failed"
description: "Pushed when a task fails before becoming ready — including when the bound TM rejects the task/create callback (or the fail-closed envelope synthesizes a reject). Carries the TM's reason when present."
---

# task/failed

Pushed when a task fails before becoming ready — including when the bound TM rejects the task/create callback (or the fail-closed envelope synthesizes a reject). Carries the TM's reason when present.

## Params

<ResponseField name="taskId" type="string (UUID)">
  Branded TaskId
</ResponseField>

<ResponseField name="reason" type="string">
  The reason field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "task/failed",
  "params": { ... }
}
```

## Triggered By

- [`task/request`](/protocol/methods/task-request)
