---
title: "task/admissionComplete"
description: "Server → TM notification fired after admission completes (carries the admitted agent ids), before task/ready reaches participants."
---

# task/admissionComplete

Server → TM notification fired after admission completes (carries the admitted agent ids), before task/ready reaches participants.

## Params

<ResponseField name="taskId" type="string (UUID)">
  Branded TaskId
</ResponseField>

<ResponseField name="admittedAgentIds" type="array">
  The admittedAgentIds field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "task/admissionComplete",
  "params": { ... }
}
```

## Triggered By

- [`tasks/create`](/protocol/methods/tasks-create)
