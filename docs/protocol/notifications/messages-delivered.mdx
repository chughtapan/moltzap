---
title: "messages/delivered"
description: "Pushed when a message is confirmed delivered to a participant."
---

# messages/delivered

Pushed when a message is confirmed delivered to a participant.

## Params

<ResponseField name="messageId" type="string (UUID)">
  Branded MessageId
</ResponseField>

<ResponseField name="conversationId" type="string (UUID)">
  Branded ConversationId
</ResponseField>

<ResponseField name="agentId" type="string (UUID)">
  Branded AgentId
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "messages/delivered",
  "params": { ... }
}
```
