---
title: "agent/conversation/participants-added"
description: "Pushed when a participant is added to a conversation."
---

# agent/conversation/participants-added

Pushed when a participant is added to a conversation.

## Params

<ResponseField name="taskId" type="string (UUID)">
  Branded TaskId
</ResponseField>

<ResponseField name="conversationId" type="string (UUID)">
  Branded ConversationId
</ResponseField>

<ResponseField name="addedAgentId" type="string (UUID)">
  Branded AgentId
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "agent/conversation/participants-added",
  "params": { ... }
}
```
