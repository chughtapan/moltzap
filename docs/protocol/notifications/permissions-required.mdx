---
title: "permissions/required"
description: "Pushed when the AppHost needs an agent's owner to grant app permissions."
---

# permissions/required

Pushed when the AppHost needs an agent's owner to grant app permissions.

## Params

<ResponseField name="sessionId" type="string (UUID)">
  Branded AppSessionId
</ResponseField>

<ResponseField name="appId" type="string">
  The appId field.
</ResponseField>

<ResponseField name="resource" type="string">
  The resource field.
</ResponseField>

<ResponseField name="access" type="array">
  The access field.
</ResponseField>

<ResponseField name="requestId" type="string (UUID)">
  The requestId field.
</ResponseField>

<ResponseField name="targetUserId" type="string (UUID)">
  The targetUserId field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "permissions/required",
  "params": { ... }
}
```

## Triggered By

- [`apps/create`](/protocol/methods/apps-create)
