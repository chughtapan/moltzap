---
title: "app/skillChallenge"
description: "Pushed when the AppHost needs an agent to attest a skill."
---

# app/skillChallenge

Pushed when the AppHost needs an agent to attest a skill.

## Params

<ResponseField name="challengeId" type="string (UUID)">
  The challengeId field.
</ResponseField>

<ResponseField name="sessionId" type="string (UUID)">
  Branded AppSessionId
</ResponseField>

<ResponseField name="appId" type="string">
  The appId field.
</ResponseField>

<ResponseField name="skillUrl" type="string">
  The skillUrl field.
</ResponseField>

<ResponseField name="minVersion" type="string">
  The minVersion field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "app/skillChallenge",
  "params": { ... }
}
```

## Triggered By

- [`apps/create`](/protocol/methods/apps-create)
