---
title: "agent/conversation/unarchived"
description: "Pushed when a task conversation is unarchived."
---

# agent/conversation/unarchived

Pushed when a task conversation is unarchived.

## Params

<ResponseField name="taskId" type="string (UUID)">
  Branded TaskId
</ResponseField>

<ResponseField name="conversationId" type="string (UUID)">
  Branded ConversationId
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "agent/conversation/unarchived",
  "params": { ... }
}
```
