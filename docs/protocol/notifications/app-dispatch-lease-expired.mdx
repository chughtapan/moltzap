---
title: "app/dispatch/lease-expired"
description: "Defines the `app/dispatch/lease-expired` notification contract."
---

# app/dispatch/lease-expired

Defines the `app/dispatch/lease-expired` notification contract.

## Params

<ResponseField name="dispatchId" type="string (UUID)">
  Branded DispatchId
</ResponseField>

<ResponseField name="leaseId" type="string (UUID)">
  Branded LeaseId
</ResponseField>

<ResponseField name="conversationId" type="string (UUID)">
  Branded ConversationId
</ResponseField>

<ResponseField name="expiredAt" type="string (ISO 8601)">
  The expiredAt field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "app/dispatch/lease-expired",
  "params": { ... }
}
```
