---
title: "app/sessionFailed"
description: "Pushed when an app session fails before becoming ready."
---

# app/sessionFailed

Pushed when an app session fails before becoming ready.

## Params

<ResponseField name="sessionId" type="string (UUID)">
  Branded AppSessionId
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "app/sessionFailed",
  "params": { ... }
}
```

## Triggered By

- [`apps/create`](/protocol/methods/apps-create)
