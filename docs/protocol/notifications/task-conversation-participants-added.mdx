---
title: "task/conversation/participants/added"
description: "Pushed when an agent is added to a conversation."
---

# task/conversation/participants/added

Pushed when an agent is added to a conversation.

## Params

<ResponseField name="taskId" type="string (UUID)">
  Branded TaskId
</ResponseField>

<ResponseField name="conversationId" type="string (UUID)">
  Branded ConversationId
</ResponseField>

<ResponseField name="addedAgentId" type="string (UUID)">
  Branded AgentId
</ResponseField>

<ResponseField name="byAgentOrTm" type="tm">
  The byAgentOrTm field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "task/conversation/participants/added",
  "params": { ... }
}
```

## Triggered By

- [`task/conversation/participants/add`](/protocol/methods/task-conversation-participants-add)
