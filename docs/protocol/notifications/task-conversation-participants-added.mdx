---
title: "task/conversation/participants/added"
description: "Pushed when a participant is added to a task conversation."
---

# task/conversation/participants/added

Pushed when a participant is added to a task conversation.

## Params

<ResponseField name="taskId" type="string (UUID)">
  Branded TaskId
</ResponseField>

<ResponseField name="conversationId" type="string (UUID)">
  Branded ConversationId
</ResponseField>

<ResponseField name="addedAgentId" type="string (UUID)">
  Branded AgentId
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "task/conversation/participants/added",
  "params": { ... }
}
```
