---
title: "task/conversation/participants/removed"
description: "Pushed when an agent is removed from a conversation (explicit, or task/leave, or dispatch deny)."
---

# task/conversation/participants/removed

Pushed when an agent is removed from a conversation (explicit, or task/leave, or dispatch deny).

## Params

<ResponseField name="taskId" type="string (UUID)">
  Branded TaskId
</ResponseField>

<ResponseField name="conversationId" type="string (UUID)">
  Branded ConversationId
</ResponseField>

<ResponseField name="removedAgentId" type="string (UUID)">
  Branded AgentId
</ResponseField>

<ResponseField name="reason" type="tm_remove | task_leave">
  The reason field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "task/conversation/participants/removed",
  "params": { ... }
}
```

## Triggered By

- [`task/conversation/participants/remove`](/protocol/methods/task-conversation-participants-remove)
- [`task/leave`](/protocol/methods/task-leave)
