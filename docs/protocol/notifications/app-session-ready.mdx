---
title: "app/sessionReady"
description: "Pushed when all required agents are admitted and the app session is active."
---

# app/sessionReady

Pushed when all required agents are admitted and the app session is active.

## Params

<ResponseField name="sessionId" type="string (UUID)">
  Branded AppSessionId
</ResponseField>

<ResponseField name="conversations" type="object (map)">
  The conversations field.
</ResponseField>

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "app/sessionReady",
  "params": { ... }
}
```

## Triggered By

- [`apps/create`](/protocol/methods/apps-create)
