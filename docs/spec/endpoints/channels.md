# Superseded: channels

This historical endpoint name is superseded by
[`daemon.md`](/spec/endpoints/daemon). Gate 1 has one shared endpoint daemon per
`AgentId`; harness-specific code is a thin runtime bridge, not a
network channel or protocol owner.
