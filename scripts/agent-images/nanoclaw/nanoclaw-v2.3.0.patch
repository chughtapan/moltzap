diff --git a/container/agent-runner/src/destinations.ts b/container/agent-runner/src/destinations.ts
index 5bf3fc7..77da6b3 100644
--- a/container/agent-runner/src/destinations.ts
+++ b/container/agent-runner/src/destinations.ts
@@ -6,9 +6,13 @@
  * queries the table live on every lookup, so admin changes take effect
  * immediately — no restart required.
  *
- * This table is BOTH the routing map and the container-visible ACL.
- * The host re-validates on the delivery side against the central DB,
- * so even if this table is stale the host's enforcement is authoritative.
+ * This table is both the routing map and the container-visible ACL for
+ * NanoClaw-owned named destinations. An explicit MoltZap address input is
+ * validated by Client and reaches the registered channel without a table row.
+ * The host re-validates every other route against the central DB.
  */
+import { MessageAddressInput } from '@moltzap/client';
+import { Schema } from 'effect';
+
 import { getAgentMailbox } from './mailbox/index.js';
 import type { Destination } from './mailbox/types.js';
@@ -44,6 +48,20 @@ export function findByName(name: string): DestinationEntry | undefined {
   return destination && destinationEntry(destination);
 }
-
+
+/** Resolve an explicit MoltZap address before any local destination alias. */
+export function findMessageDestination(name: string): DestinationEntry | undefined {
+  if (Schema.is(MessageAddressInput)(name)) {
+    return {
+      name,
+      displayName: name,
+      type: 'channel',
+      channelType: 'moltzap',
+      platformId: name,
+    };
+  }
+  return findByName(name);
+}
+
 export function findByRouting(
   channelType: string | null | undefined,
   platformId: string | null | undefined,
@@ -77,8 +95,7 @@ function buildDestinationsSection(mode: SessionMode): string {
   const lines = ['## Sending messages', ''];
-
+
   if (all.length === 0) {
-    lines.push('You currently have no configured destinations. You cannot send messages until an admin wires one up.');
-    if (mode.kind === 'chat') return lines.join('\n');
+    lines.push('You currently have no configured named destinations. You may still send to an explicit MoltZap `agent:name` or `group:name,...` address supplied by the request.');
   } else if (all.length === 1) {
     const d = all[0];
     lines.push(`Your destination is \`${d.name}\`${destinationLabel(d)}.`);
@@ -89,11 +106,15 @@ function buildDestinationsSection(mode: SessionMode): string {
     }
   }
-
+
-  lines.push('');
+  lines.push(
+    '',
+    'A supplied MoltZap `agent:name` or `group:name,...` address is also a valid explicit destination. Client validates and canonicalizes it.',
+    '',
+  );
-
+
   if (mode.kind === 'task') {
     lines.push(
-      'This is an isolated task run with no attached chat. Only notify someone when the task asks you to. For a user-visible message, call `send_message({ to: "name", text: "..." })`; for a file, call `send_file` with `to`. Always pass the explicit named destination.',
+      'This is an isolated task run with no attached chat. Only notify someone when the task asks you to. For a user-visible message, call `send_message({ to: "name-or-address", text: "..." })`; for a file, call `send_file` with `to`. Always pass the supplied named destination or MoltZap address.',
       '',
       `Your final output is not sent to the user. End with a concise work-log summary. It is recorded automatically in \`tasks/${mode.taskId}.md\`. Read that file when you need context from earlier runs. Use \`ncl tasks append-log --msg "…"\` only for optional mid-run notes.`,
     );
@@ -101,7 +122,7 @@ function buildDestinationsSection(mode: SessionMode): string {
   }
-
+
   lines.push(
-    'Wrap each delivered message in a `<message to="name">…</message>` block; include several blocks in one response to address several destinations. `<internal>…</internal>` marks thinking you don\'t want sent.',
+    'Wrap each delivered message in a `<message to="name-or-address">…</message>` block; include several blocks in one response to address several destinations. `<internal>…</internal>` marks thinking you don\'t want sent.',
   );
   lines.push('');
   lines.push(
diff --git a/container/agent-runner/src/mcp-tools/core.ts b/container/agent-runner/src/mcp-tools/core.ts
index 1bf9f2c..1acbbf3 100644
--- a/container/agent-runner/src/mcp-tools/core.ts
+++ b/container/agent-runner/src/mcp-tools/core.ts
@@ -9,7 +9,7 @@
 import fs from 'fs';
 import path from 'path';
-
+
-import { findByName, getAllDestinations } from '../destinations.js';
+import { findByName, findMessageDestination, getAllDestinations, type DestinationEntry } from '../destinations.js';
 import { getMessageIdBySeq, getRoutingBySeq, writeMessageOut } from '../db/messages-out.js';
 import { getCurrentInReplyTo } from '../db/session-state.js';
 import { getSessionRouting } from '../db/session-routing.js';
@@ -48,8 +48,9 @@ function destinationList(): string {
  */
 function resolveRouting(
   to: string,
+  resolveDestination: (name: string) => DestinationEntry | undefined = findByName,
 ): { channel_type: string; platform_id: string; thread_id: string | null; resolvedName: string } | { error: string } {
-  const dest = findByName(to);
+  const dest = resolveDestination(to);
   if (!dest) return { error: `Unknown destination "${to}". Known: ${destinationList()}` };
   if (dest.type === 'channel') {
     // If the destination is the same channel the session is bound to,
@@ -70,13 +71,13 @@ function resolveRouting(
 export const sendMessage: McpToolDefinition = {
   tool: {
     name: 'send_message',
-    description: 'Send a message to a named destination.',
+    description: 'Send a message to a named destination or an explicit MoltZap agent: or group: address.',
     inputSchema: {
       type: 'object' as const,
       properties: {
         to: {
           type: 'string',
-          description: 'Destination name (e.g., "family", "worker-1").',
+          description: 'Destination name or explicit MoltZap agent: or group: address.',
         },
         text: { type: 'string', description: 'Message content' },
       },
@@ -89,7 +90,7 @@ export const sendMessage: McpToolDefinition = {
     if (!to) return err(`to is required. Options: ${destinationList()}`);
     if (!text) return err('text is required');
-
+
-    const routing = resolveRouting(to);
+    const routing = resolveRouting(to, findMessageDestination);
     if ('error' in routing) return err(routing.error);
-
+
     const id = generateId();
diff --git a/container/agent-runner/src/poll-loop.ts b/container/agent-runner/src/poll-loop.ts
index c7b5de0..6ea44ee 100644
--- a/container/agent-runner/src/poll-loop.ts
+++ b/container/agent-runner/src/poll-loop.ts
@@ -1,4 +1,4 @@
-import { findByName, getAllDestinations, type DestinationEntry } from './destinations.js';
+import { findMessageDestination, getAllDestinations, type DestinationEntry } from './destinations.js';
 import {
   getPendingMessages,
   markProcessing,
@@ -830,7 +830,7 @@ export async function deliverMidTurnBlocks(
     const toName = match[1];
     const rawBody = match[2];
     const body = stripHarnessTagArtifacts(rawBody.trim());
-    const dest = findByName(toName);
+    const dest = findMessageDestination(toName);
     if (!dest) continue;
     // Never deliver a blank message: a body that is empty (or was only
     // harness-tag artifacts) is skipped here; the result path logs it.
@@ -1015,7 +1015,7 @@ export async function dispatchResultText(
       taskBlocks.push({ to: toName, body });
       continue;
     }
-    const dest = findByName(toName);
+    const dest = findMessageDestination(toName);
     if (!dest) {
       log(`Unknown destination in <message to="${toName}">, dropping block`);
       scratchpadParts.push(`[dropped: unknown destination "${toName}"] ${body}`);
diff --git a/src/delivery.ts b/src/delivery.ts
index 5e2c028..71f8abd 100644
--- a/src/delivery.ts
+++ b/src/delivery.ts
@@ -3,6 +3,9 @@
  * SQLite reads runner-owned outbound state read-only and records delivery in
  * host-owned inbound state; other implementations preserve that ownership.
  */
+import { MessageAddressInput } from '@moltzap/client';
+import { Schema } from 'effect';
+
 import {
   getRunningSessions,
   getActiveSessions,
@@ -344,8 +347,8 @@ async function deliverMessage(
     return;
   }
-
+
-  // Permission check: the source agent must be allowed to deliver to this
-  // channel destination. Two ways it passes:
+  // NanoClaw-owned channel destinations pass the local permission check in one
+  // of two ways:
   //
   //   1. The target is the session's own origin chat (session.messaging_group_id
   //      matches). An agent can always reply to the chat it was spawned from;
@@ -356,12 +359,14 @@ async function deliverMessage(
   //      these automatically when wiring, so an operator wiring additional
   //      chats to the agent doesn't need a separate ACL step.
   //
-  // Failures throw — unlike a silent `return`, an Error falls into the retry
-  // path in deliverSessionMessages and eventually marks the message as failed
-  // (instead of marking it delivered when nothing was actually delivered,
-  // which was the pre-refactor bug).
+  // A syntactically valid explicit MoltZap address already contains the
+  // complete Client route. It bypasses NanoClaw's messaging-group lookup and
+  // named-destination ACL, then Client validates and canonicalizes it.
+  //
+  // Failures throw into deliverSessionMessages retry handling.
   let deliverInstance: string | undefined;
-  if (msg.channelType && msg.platformId) {
+  const explicitMoltZapRoute = msg.channelType === 'moltzap' && Schema.is(MessageAddressInput)(msg.platformId);
+  if (msg.channelType && msg.platformId && !explicitMoltZapRoute) {
     // Resolve the messaging group ORIGIN-SESSION-FIRST: when the message
     // targets the session's own chat address, the origin row wins even if
     // sibling instances share the same (channel_type, platform_id) — so the
diff --git a/src/index.ts b/src/index.ts
index 8e03a89..583efe4 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -109,12 +109,13 @@ async function main(): Promise<void> {
         });
       },
       onInboundEvent(event) {
-        routeInbound(event).catch((err) => {
+        return routeInbound(event).catch((err) => {
           log.error('Failed to route inbound event', {
             sourceAdapter: adapter.channelType,
             targetChannelType: event.channelType,
             err,
           });
+          throw err;
         });
       },
       onMetadata(platformId, name, isGroup) {
